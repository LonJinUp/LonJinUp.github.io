<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://lonjinup.github.io</id>
    <title>陇锦</title>
    <updated>2019-08-04T17:05:48.239Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://lonjinup.github.io"/>
    <link rel="self" href="https://lonjinup.github.io/atom.xml"/>
    <subtitle>记录学习 记录生活</subtitle>
    <logo>https://lonjinup.github.io/images/avatar.png</logo>
    <icon>https://lonjinup.github.io/favicon.ico</icon>
    <rights>All rights reserved 2019, 陇锦</rights>
    <entry>
        <title type="html"><![CDATA[搭建shadowsocks服务]]></title>
        <id>https://lonjinup.github.io/post/shadowsocks</id>
        <link href="https://lonjinup.github.io/post/shadowsocks">
        </link>
        <updated>2019-08-01T05:24:58.000Z</updated>
        <content type="html"><![CDATA[<h1 id="shadowsocks">shadowsocks</h1>
<pre><code>之前一直是公司后端用公司服务器搭建的，网速慢的要死，准备买个星域的，但是网站老打不开，还是自己搭一个吧，价格也不贵，一个月折合人民币￥25，网速也不慢。
</code></pre>
<h2 id="一-购买服务器">一、购买服务器</h2>
<ul>
<li>
<p>国外的服务器比较多，像我们常见的就有搬瓦工，vultr，linode 亚马逊等，这里我选择<a href="https://app.cloudcone.com">cloudcone</a>。</p>
<h3 id="注册">注册</h3>
</li>
<li>
<p>这里选择<a href="https://app.cloudcone.com">cloudcone</a>进行账号注册。</p>
<h3 id="充值">充值</h3>
</li>
<li>
<p>这里支持支付宝充值，最低充值金额为 <strong>$5</strong>，找到充值入口<img src="https://lonjinup.github.io/post-images/1564638465290.png" alt=""></p>
</li>
<li>
<p>输入金额支付宝支付<br>
<img src="https://lonjinup.github.io/post-images/1564638814320.png" alt=""></p>
<h3 id="购买">购买</h3>
</li>
<li>
<p>首页点击新建<br>
<img src="https://lonjinup.github.io/post-images/1564638680586.png" alt=""></p>
</li>
<li>
<p>按图上配置即可，只需$3.71。<br>
<img src="https://lonjinup.github.io/post-images/1564639093205.png" alt=""></p>
</li>
</ul>
<blockquote>
<pre><code>这时候购买完毕就可以开始搭建啦，服务器IP地址和密码等信息会下发到邮箱。
</code></pre>
</blockquote>
<h2 id="二-搭建">二、搭建</h2>
<h3 id="准备工具">准备工具</h3>
<h4 id="xshell6"><a href="https://pan.baidu.com/s/1IQcvbkbNWfs44_PDxQhxtQ#list/path=%2F">Xshell6</a>，百度网盘下载。提取码<strong>y4tq</strong></h4>
<h4 id="shadowsocks客户端">shadowsocks客户端</h4>
<p><a href="https://github.com/shadowsocks/shadowsocks-windows">Windows客户端</a><br>
<a href="https://github.com/shadowsocks/ShadowsocksX-NG">Mac客户端</a><br>
<a href="https://github.com/shadowsocks/shadowsocks-android">Android客户端</a><br>
iOS可以用美区ID下载<strong>Potatso Lite</strong>，美国账号可以去万能的tb购买。</p>
<h3 id="服务器">服务器</h3>
<h4 id="连接服务器">连接服务器</h4>
<ul>
<li>安装好<strong>Xshell</strong>，通过ip地址和密码连接服务器。这里就不截图了。</li>
</ul>
<h4 id="安装python-pip">安装python-pip</h4>
<ul>
<li>安装 python-pip<br>
<code>yum -y install python-pip</code></li>
</ul>
<blockquote>
<p>如果提示No package python-pip available.Error: Nothing to do因为没有此rpm包，此包包含在pel源里面,执行如下代码:</p>
</blockquote>
<p><code>rpm -ivh http://dl.fedoraproject.org/pub/epel/6/i386/epel-release-6-8.noarch.rpm</code></p>
<ul>
<li>安装成功后会提示Complete! 之后清除一下缓存<br>
<code>yum clean al</code></li>
</ul>
<h4 id="安装shadowsocks">安装shadowsocks</h4>
<p><code>pip install shadowsocks</code></p>
<h4 id="配置信息">配置信息</h4>
<ul>
<li>新建文件<br>
<code>vim /etc/shadowsocks.json</code></li>
<li>输入配置信息</li>
</ul>
<pre><code>{
  &quot;server&quot;: &quot;服务器IP&quot;,
  &quot;port_password&quot;: {
    &quot;端口&quot;: &quot;密码&quot;
  },
  &quot;local_address&quot;: &quot;127.0.0.1&quot;,
  &quot;local_port&quot;: 1080,
  &quot;timeout&quot;: 300,
  &quot;method&quot;: &quot;aes-256-cfb&quot;
}
</code></pre>
<ul>
<li>编辑好后esc退出输入:wq!保存。</li>
</ul>
<h4 id="运行">运行</h4>
<ul>
<li>要在前台运行:<br>
<code>ssserver -c /etc/shadowsocks.json</code></li>
<li>要在后台运行：<br>
<code>ssserver -c /etc/shadowsocks.json -d start</code><br>
<code>ssserver -c /etc/shadowsocks.json -d stop</code></li>
</ul>
<h2 id="三客户端连接">三.客户端连接</h2>
<ul>
<li>打开客户端，输入ip、端口、选择加盟方式、点击确定即可连接，快试试能不能打开<a href="https://www.google.com/search?ei=24VCXdm0Dtf1-gSdi6iQDg&amp;q=pronhub&amp;oq=pronhub&amp;gs_l=psy-ab.3..35i39.2258667.2261361..2261596...1.0..0.818.2559.2-5j0j1j0j1......0....1..gws-wiz.....10..0i71j0.v5vb1A7VS1c&amp;ved=0ahUKEwiZjvL3hOHjAhXXup4KHZ0FCuIQ4dUDCAo&amp;uact=5">Google</a>吧！</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JavaScript之原型and原型链]]></title>
        <id>https://lonjinup.github.io/post/yuan-xing-yu-yuan-xing-lian</id>
        <link href="https://lonjinup.github.io/post/yuan-xing-yu-yuan-xing-lian">
        </link>
        <updated>2019-07-23T09:24:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="为什么需要原型及原型链">为什么需要原型及原型链？</h2>
<ul>
<li>demo</li>
</ul>
<pre><code class="language-js">    function Person(name,age){
       
       this.name=name;
       this.age=age;
       this.eat=function(){
           console.log(age+&quot;岁的&quot;+name+&quot;在吃饭&quot;)
       }
   };

   let Tom=new Person(&quot;Tom&quot;,24);
   let Bob=new Person(&quot;Tom&quot;,24);
   
   console.log(Tom.eat===Bob.eat);  //flase
</code></pre>
<ul>
<li>
<p>可以看到对于同一个函数，new出来的实例，都会开出新的一块堆区,所以例子中Tom和Bob的<strong>eat</strong>是不同的。<br>
虽然拥有自己的的东西(比如房子，车子)是很好的，但它也有不好的地方，毕竟一共就那么大的地方(内存)，你要<br>
一直建立房子，会导致地方不足(内存不足)。</p>
</li>
<li>
<p>所以要想个方法，建立共享库的的对象(例如把楼房建高),这样就可以在需要的时候调用一个类似共享库的对象(社区)，让实例能沿着某个线索找到它的归处。这个线索就是<strong>原型链(prototype)</strong></p>
</li>
</ul>
<pre><code class="language-js">
    function Person(name,age){
        this.name=name;
        this.age=age;
    };
    // 通过构造函数的 Person 的 prototype 属性找到 Person 的原型对象
    Person.prototype.eat=function(){
        console.log(&quot;吃饭&quot;)
    };

   let Tom=new Person(&quot;Tom&quot;,24);
   let Bob=new Person(&quot;Tom&quot;,24);

   console.log(Tom.eat===Bob.eat); //true

</code></pre>
<h2 id="prototype是什么">prototype是什么？</h2>
<ul>
<li>看图</li>
</ul>
<p><img src="https://s2.ax1x.com/2019/07/18/ZXsADU.png" alt="图解prototype"></p>
<ul>
<li>
<p>js说，我好寂寞，因为js本源是空的，即为null。</p>
</li>
<li>
<p>js说，要有神，所以它通过万能术 <strong>_ proto _</strong> 产生了No1号神，即:<strong>No1._ proto _=null</strong>。</p>
</li>
<li>
<p>js说，神你要有自己的想法啊，所以神自己想了个办法，根据自己的原型<strong>prototype</strong>创建了对象；<strong>Object</strong>，即<strong>Object.prototype==No1;No1._ proto _=null</strong>。于是我们把<strong>prototype</strong>叫做原型，就好比<strong>Object</strong>的原型是神，男人的原型就是人类一样。同时 <strong>_ proto _</strong> 叫做原型链，毕竟有了 <strong>_ proto _</strong> ,对象、神、js之间才有了联系，这时候<strong>Object.prototype._ proto _=null</strong>。</p>
</li>
<li>
<p>js说，神你要有更多的想法啊，我把万能术 <strong>_ proto  _</strong> 都借你用了。所以神根据 <strong>Object</strong>,使用 <strong>_ proto _</strong> 做了个机器No2,即：<strong>No2._ proto _=No1</strong>,并规定所有东西，通过 <strong>_ proto _</strong> 可以连接机器，再找到自己包括<strong>Object</strong>也是，于是<strong>Object成为了所以对象的原型</strong>，<strong>Object. _ proto  _ ._ proto _==No1</strong>,然后 <strong>String</strong>、<strong>Numer</strong>、<strong>Boolean</strong>、<strong>Array</strong>这些物种也是如此。</p>
</li>
</ul>
<p>js说，神你的机器好厉害，你的机器能不能做出更多的机器啊？神咧嘴一笑:你通过万能术创造了我，我通过原型创造了对象，如此，那我造一个机器 <strong>Function</strong> ,<strong>function.prototype == No2, function._ proto_=No2</strong>,即：<strong>function。prototype == function. _ proto_</strong>;这样No2就成了造机器的机器，他负责管理<strong>String</strong>、<strong>Numer</strong>、<strong>Boolean</strong>、<strong>Array</strong>这几个。</p>
<ul>
<li>最后，说到底我们就很容易理解那幅图，并比较了解下面的公式了。</li>
</ul>
<pre><code class="language-js">    Object._proto_ = Function.prototype;
    Function.prototype._proto_ = Object.prototype;
    Object.prototype._proto = null
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JavaScript中this指向以及改变this指向]]></title>
        <id>https://lonjinup.github.io/post/javascript-zhong-this-zhi-xiang-yi-ji-gai-bian-this-zhi-xiang</id>
        <link href="https://lonjinup.github.io/post/javascript-zhong-this-zhi-xiang-yi-ji-gai-bian-this-zhi-xiang">
        </link>
        <updated>2019-07-23T09:21:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="关于this指向">关于this指向</h2>
<ul>
<li>首先必须要说的是，<strong>this的指向在函数定义的时候是确定不了的，只有函数执行的时候才能确定this到底指向谁，实际上this的最终指向的是那个调用它的对象</strong>（这句话有些问题，后面会解释为什么会有问题，虽然网上大部分的文章都是这样说的，虽然在很多情况下那样去理解不会出什么问题，但是实际上那样理解是不准确的，所以在你理解this的时候会有种琢磨不透的感觉）</li>
</ul>
<h3 id="例子1">例子1</h3>
<pre><code class="language-js">   function name(){
        var a=&quot;A&quot;;
        console.log(this.a);//undefined
        console.log(this);//Window
    };
    name(); 
</code></pre>
<ul>
<li>按照上面所说的<strong>this</strong>始终指向的是调用它的对象，这里的函数<strong>name</strong>实际上是被Window对象所调出来的。所以<strong>this.a=undefined</strong>;</li>
</ul>
<pre><code class="language-js">    function name(){
        var a=&quot;A&quot;;
        alert(this.a);//undefined
        alert(this);//Window
    };
    name(); 
</code></pre>
<ul>
<li>和上面代码一样，<strong>alert</strong> 也是Window一个属性，也是Window调出来的。</li>
</ul>
<h2 id="例子2">例子2</h2>
<pre><code class="language-js"> var n={
        name:&quot;LonJin&quot;,
        fn:function(){
            console.log(this.name)//LonJin
        }
    };
    n.fn();
</code></pre>
<ul>
<li>
<p>这里的this指向的是对象<strong>n</strong>，因为你调用这个fn是通过<strong>n.fn()<strong>执行的，那自然指向就是对象</strong>n</strong>，这里再次强调一点，this的指向在函数创建的时候是决定不了的，在调用的时候才能决定，谁调用的就指向谁，一定要搞清楚这个。</p>
</li>
<li>
<p>其实例子1和例子2说的并不够准确，下面这个例子就可以推翻上面的理论。</p>
</li>
</ul>
<h2 id="例子3">例子3</h2>
<pre><code class="language-js"> var n={
        name:&quot;LonJin&quot;,
        fn:function(){
            console.log(this.name)//LonJin
        }
    };
    window.n.fn();
</code></pre>
<ul>
<li>
<p>这段代码和上面的那段代码几乎是一样的，但是这里的this为什么不是指向window，如果按照上面的理论，最终this指向的是调用它的对象，这里先说个而外话，window是js中的全局对象，我们创建的变量实际上是给window添加属性，所以这里可以用<strong>window.n.fn()</strong>。</p>
</li>
<li>
<p>再来看一段代码</p>
</li>
</ul>
<pre><code class="language-js">    var n={
        a:10,
        b:{
            a:12,
            fn:function(){
                console.log(this.a)//12
            }
        }
    };
    n.b.fn();
</code></pre>
<ul>
<li>这里同样也是对象<strong>n</strong>点出来的，但是同样this并没有执行它，那你肯定会说我一开始说的那些不就都是错误的吗？其实也不是，只是一开始说的不准确，接下来我将补充一句话，我相信你就可以彻底的理解this的指向的问题。</li>
</ul>
<h2 id="彻底理解this指向">彻底理解this指向</h2>
<ul>
<li>
<p>情况1：如果一个函数中有this，但是它没有被上一级的对象所调用，那么this指向的就是window，这里需要说明的是在js的严格版中this指向的不是window，但是我们这里不探讨严格版的问题，你想了解可以自行上网查找。</p>
</li>
<li>
<p>情况2：如果一个函数中有this，这个函数有被上一级的对象所调用，那么this指向的就是上一级的对象。</p>
</li>
<li>
<p>情况3：如果一个函数中有this，这个函数中包含多个对象，尽管这个函数是被最外层的对象所调用，this指向的也只是它上一级的对象，例子3可以证明，如果不相信，那么接下来我们继续看几个例子。</p>
</li>
</ul>
<pre><code class="language-js">    var n={
        a:10,
        b:{
            // a:12,
            fn:function(){
                console.log(this.a)//undefined
            }
        }
    };
    n.b.fn();
</code></pre>
<ul>
<li>尽管对象b中没有属性a，这个this指向的也是对象b，因为this只会指向它的上一级对象，不管这个对象中有没有this要的东西。还有一种特殊的情况，请看例子4。</li>
</ul>
<p>##例子4</p>
<pre><code class="language-js">    var n={
        a:10,
        b:{
            a:12,
            fn:function(){
                console.log(this.a)//undefined
                console.log(this);//window
            }
        }
    };
    var j=n.b.fn;
    j();
</code></pre>
<ul>
<li>在这里this指向window，这里就又不明白了，其实还是不理解<strong>this永远指向最后调用它的对象</strong>，也就是看它执行的时候是谁调用的它，例子4中虽然<strong>函数fn</strong>是被<strong>对象b</strong>所引用，但是在将<strong>fn</strong>赋值给变量<strong>j</strong>的时候并没有执行所以最终指向的是window，这里和例子3是不一样的，例子3是直接执行了<strong>fn</strong>。</li>
</ul>
<h3 id="当this遇到retrun时候">当this遇到retrun时候</h3>
<pre><code class="language-js"> function fn(){
       this.user=&quot;LonJin&quot;;
       return {}
   };
   var a=new fn;
   console.log(a.user);//underfind
</code></pre>
<ul>
<li>再看一个</li>
</ul>
<pre><code class="language-js">  function fn(){
       this.user=&quot;LonJin&quot;;
       return function(){
           
       }
   };
   var a=new fn;
   console.log(a.user);//undefined
</code></pre>
<ul>
<li>继续看</li>
</ul>
<pre><code class="language-js">   function fn(){
       this.user=&quot;LonJin&quot;;
       return 1
   };
   var a=new fn;
   console.log(a.user);//LonJin
</code></pre>
<pre><code class="language-js">   function fn(){
       this.user=&quot;LonJin&quot;;
       return undefined
   };
   var a=new fn;
   console.log(a.user);//LonJin
</code></pre>
<ul>
<li>
<p>如果返回值是一个对象，那么this指向的就是那个返回的对象，如果返回值不是一个对象那么this还是指向函数的实例。</p>
</li>
<li>
<p>还有一点就是虽然null也是对象，但是在这里this还是指向那个函数的实例，因为null比较特殊。</p>
</li>
</ul>
<pre><code class="language-js">   function fn(){
       this.user=&quot;LonJin&quot;;
       return null
   };
   var a=new fn;
   console.log(a.user);//LonJin
</code></pre>
<h3 id="补充">补充</h3>
<ul>
<li>在严格版中的默认的this不再是window，而是undefined。</li>
</ul>
<h2 id="如何改变this指向">如何改变this指向</h2>
<pre><code class="language-js"> function fn(){
       this.user=&quot;LonJin&quot;;
   };
   var a=new fn();
   console.log(a.user);//LonJin
</code></pre>
<ul>
<li>
<p>为什么this会指向a？首先new关键字会创建一个空的对象，然后会自动调用一个函数apply方法，将this指向这个空对象，这样的话函数内部的this就会被这个空的对象替代。</p>
</li>
<li>
<p>箭头函数在自己的作用域内不绑定 this，即没有自己的 this，如果要使用 this ，就会指向定义时所在的作用域的 this 值。</p>
</li>
</ul>
]]></content>
    </entry>
</feed>