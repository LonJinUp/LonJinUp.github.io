<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://lonjinup.github.io</id>
    <title>陇锦</title>
    <updated>2020-05-22T05:43:19.880Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://lonjinup.github.io"/>
    <link rel="self" href="https://lonjinup.github.io/atom.xml"/>
    <subtitle>记录学习 记录生活</subtitle>
    <logo>https://lonjinup.github.io/images/avatar.png</logo>
    <icon>https://lonjinup.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, 陇锦</rights>
    <entry>
        <title type="html"><![CDATA[【ES6】class类]]></title>
        <id>https://lonjinup.github.io/post/es6-class-lei</id>
        <link href="https://lonjinup.github.io/post/es6-class-lei">
        </link>
        <updated>2020-05-22T05:41:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="class">class</h2>
<h3 id="es5中创建对象以及继承">ES5中创建对象以及继承</h3>
<pre><code class="language-js">    function Car(options){
        this.title=options.title;
    };

    Car.prototype.drive=function(){
        return 'run'
    };

    const car =new Car({title:'BMW'});

    console.log(car); 

    console.log(car.drive());

</code></pre>
<h4 id="继承">继承</h4>
<pre><code class="language-js">   function Toyota(options){
       
         this.color=options.color;
    };

    const toyota=new Toyota({color:'RED',title:'hello'});

    console.log(toyota.title)//undefined

   

</code></pre>
<ul>
<li>因为title没有定义，所以拿不到,需要借助<strong>Call</strong></li>
</ul>
<pre><code class="language-js">
    function Toyota(options){
       Car.call(this,options)
       this.color=options.color;
   };
   const toyota=new Toyota({color:'RED',title:'hello'});

   console.log(toyota)；


</code></pre>
<ul>
<li>如果想继承上面中drive方法</li>
</ul>
<pre><code class="language-js">
    function Toyota(options){
       Car.call(this,options)
       this.color=options.color;
    };

   Toyota.prototype=Object.create(Car.prototype);
   
   Toyota.prototype.constructor=Toyota;

   const toyota=new Toyota({color:'RED',title:'hello'});

   console.log(toyota.drive());

</code></pre>
<h3 id="es6中创建对象以及继承">ES6中创建对象以及继承</h3>
<pre><code class="language-js">    class Car{
        //构造函数
        constructor({title}){
            this.title=title
        }
       drive(){
           return 'runing'
       }
    };

    const car=new Car({title:'bmw'});

    console.log(car)
</code></pre>
<h3 id="继承-2">继承</h3>
<pre><code class="language-js">   class Toyota{
       constructor(e){
           
            this.color=e.color
       };

   };

   const toyota=new Toyota({color:&quot;red&quot;,title:'hello'});
   console.log(toyota);

</code></pre>
<ul>
<li>但接收不到title</li>
</ul>
<pre><code class="language-js">   class Toyota{
       constructor(e){
            super(e);//接收器
            this.color=e.color
       };

   }
   const toyota=new Toyota({color:&quot;red&quot;,title:'hello'});
   console.log(toyota);//Uncaught SyntaxError: 'super' keyword unexpected here

</code></pre>
<ul>
<li>上面因为因为没有父级，没有告诉它继承于谁</li>
</ul>
<pre><code class="language-js">    class Toyota extends Car{
       constructor(e){
            super(e);//接收器
            this.color=e.color
       };
 
    }；

   const toyota=new Toyota({color:&quot;red&quot;,title:'hello'});
   
   console.log(toyota)

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【01】React环境搭建以及文件目录介绍]]></title>
        <id>https://lonjinup.github.io/post/01-react</id>
        <link href="https://lonjinup.github.io/post/01-react">
        </link>
        <updated>2020-01-18T15:46:12.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<p>打算学习一下React，记录一下学习遇到的问题。</p>
</blockquote>
<h2 id="搭建环境">搭建环境</h2>
<h3 id="环境">环境</h3>
<h4 id="安装node">安装Node</h4>
<ul>
<li>到 <a href="https://nodejs.org/zh-cn/">Node官网</a>下载，下载完直接根据提示完成安装。window电脑打开<strong>CMD</strong>，Mac<br>
电脑打开<strong>终端</strong>，输入<strong>node -v</strong>可以查看版本号，说明安装成功了。</li>
</ul>
<pre><code class="language-js">  node -v
</code></pre>
<h4 id="react脚手架">React脚手架</h4>
<ul>
<li>打开终端，输入下面命令就开始安装了。</li>
</ul>
<pre><code class="language-js">    npm install -g create-react-app
</code></pre>
<ul>
<li>如果你是Mac，需要在前面加<strong>sudo</strong>，然后按提示输入密码进行安装。不然会提示你没有权限。</li>
</ul>
<pre><code class="language-js">    sudo npm install -g create-react-app
</code></pre>
<h2 id="创建我的第一个项目">创建我的第一个项目</h2>
<ul>
<li>进入到你想创建项目的文件夹</li>
</ul>
<pre><code class="language-js">    create-react-app demo01   //用脚手架创建React项目,后面是项目名字
    cd demo01   //等创建完成后，进入项目目录
    npm start   //预览项目，如果能正常打开，说明项目创建成功
</code></pre>
<ul>
<li>创建成功会自动打开浏览器，能正常显示React图标，就说明创建成功了！</li>
</ul>
<h2 id="目录讲解">目录讲解</h2>
<pre><code class="language-js">
--- README.md (项目说明文件，工作中可以写项目描述，项目说明等)

--- package.json (这个文件是webpack配置和项目包管理文件，项目中依赖的第三方包和一些常用命令配置都在这个里边进行配置目前位置，我们不需要改动。)

--- package-lock.json (锁定安装时的版本号，并且需要上传到git，以保证其他人再npm install 时大家的依赖能保证一致。)

--- gitignore (这个是git的选择性上传的配置文件，比如一会要介绍的node_modules文件夹，就需要配置不上传。)

--- node_modules (这个文件夹就是我们项目的依赖包，到目前位置，脚手架已经都给我们下载好了，你不需要单独安装什么。)

--- public (公共文件)
------- favicon.ico （网站标签小图标）
------- index.html (模版文件,其中&lt;noscript&gt;&lt;/noscript&gt;标签是容错处理，如果当前JavaScript没有跑通的情况下就会显示里面的内容)
------- manifest.json （移动端配置文件）

--- src (开放的源代码，我们平时操作做最多的目录)
------- index.js (项目入口文件)
------- ndex.css （这个是index.js里的CSS文件。）
------- app.js （这个文件相当于一个方法模块，也是一个简单的模块化编程。）
------- serviceWorker.js （这个是用于写移动端开发的，PWA必须用到这个文件，有了这个文件，就相当于有了离线浏览的功能。）
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【ES6】解构(数组-对象)]]></title>
        <id>https://lonjinup.github.io/post/es6-jie-gou-shu-zu-dui-xiang</id>
        <link href="https://lonjinup.github.io/post/es6-jie-gou-shu-zu-dui-xiang">
        </link>
        <updated>2020-01-09T16:01:01.000Z</updated>
        <content type="html"><![CDATA[<h1 id="解构">解构</h1>
<blockquote>
<p>主要应用于两种类型，一种是数组，一种是对象。</p>
</blockquote>
<h2 id="对象解构">对象解构</h2>
<ul>
<li>看一个例子,如果要取到对象中的某个值，我们需要这样操作</li>
</ul>
<pre><code class="language-js">
    var expense={
        type:'es6',
        amount:'45'
    };

    var type=expense.type;

    var amount=expense.amount;

    console.log(type,amount); //es6,45

</code></pre>
<ul>
<li>使用解构的话，我们可以这么写</li>
</ul>
<pre><code class="language-js">
    var expense={
        type:'es6',
        amount:'45'
    };

    const {type,amount}=expense;
    console.log(type,amount);//es6,45

</code></pre>
<ul>
<li>如果对象中没有该属性，则会返回</li>
</ul>
<pre><code class="language-js">
    var expense={
        type:'es6',
        amount:'45'
    };

     const {type,amount,abc}=expense;
     console.log(type,amount,abc);//es6 45 undefined
</code></pre>
<h3 id="小案例">小案例</h3>
<blockquote>
<p>获取对象中定义的某些值</p>
</blockquote>
<ul>
<li>ES5实现</li>
</ul>
<pre><code class="language-js">    var saveFiled={
        extension:'jpg',
        name:'es6',
        size:'1024'
    };

    function fileSammary(file) {
        return `${file.name}.${file.extension}的大小是${file.size}`
    };

    console.log(fileSammary(saveFiled));//es6.jpg的大小是1024
</code></pre>
<ul>
<li>ES6实现</li>
</ul>
<pre><code class="language-js">
    var saveFiled={
        extension:'jpg',
        name:'es6',
        size:'1024'
    };

    function fileSammary({name,extension,size}){
            return `${name}.${extension}的大小是${size}`
    };

    console.log(fileSammary(saveFiled));//es6.jpg的大小是1024
</code></pre>
<h2 id="数组的解构">数组的解构</h2>
<ul>
<li>直接上例子，获取数组的值</li>
</ul>
<pre><code class="language-js">
    const names=['tom','bob','nike'];

    const [name1,name2,name3]=names;

    console.log(name1,name2,name3)//tom bob nike
</code></pre>
<ul>
<li>如果想要获取数组中的长度，直接在花括号中输入 <strong>length</strong>；</li>
</ul>
<pre><code class="language-js">
     const names=['tom','bob','nike'];
    
     const {length}=names;
    
     console.log(length)//3
</code></pre>
<h3 id="展开运算符进行解构">展开运算符进行解构</h3>
<pre><code class="language-js">
    const names=['tom','bob','nike'];

    const [name,...rest]=names;

    console.log(name)  //tom;
    console.log(rest) // [&quot;bob&quot;, &quot;nike&quot;]

</code></pre>
<p>还可以这么操作</p>
<pre><code class="language-js">
    let a,b;
   
    [a,b]=[100,200];

    console.log(a,b);
</code></pre>
<h2 id="对象数组解构">对象数组解构</h2>
<blockquote>
<p>获取对象数组中的某个属性</p>
</blockquote>
<ul>
<li>ES5</li>
</ul>
<pre><code class="language-js">    const people=[
        {names:&quot;tom&quot;,age:20},
        {names:&quot;bob&quot;,age:10},
        {names:&quot;nike&quot;,age:24},
    ];

     var age=people[0].age;
     console.log(age);//20
</code></pre>
<ul>
<li>ES6</li>
</ul>
<pre><code class="language-js">
    const people=[
        {names:&quot;tom&quot;,age:20},
        {names:&quot;bob&quot;,age:10},
        {names:&quot;nike&quot;,age:24},
    ];

     //二次解构
     const [{age}]=people;
    
     console.log(age)//20
</code></pre>
<h2 id="使用场景">使用场景</h2>
<blockquote>
<p>将数组转化为对象</p>
</blockquote>
<pre><code class="language-js">    const point=[
        [4,5],
        [10,1],
        [0,10]
    ];

    //期望数据格式

    // [
    //     {x:4,y:5},
    //     {x:10,y:1},
    //     {x:0,y:10}
    // ]

    let newPoint=point.map(pair=&gt;{
        const x=pair[0];
        const y=pair[1];

        return {x,y}
    })

    console.log(newPoint)
</code></pre>
<ul>
<li>还可以进行优化一下</li>
</ul>
<pre><code class="language-js">
    let newPoint=point.map(pair=&gt;{
       
         const [x,y]=pair;

         return {x,y}
    });

    console.log(newPoint)
</code></pre>
<ul>
<li>再一步优化</li>
</ul>
<pre><code class="language-js">
   let newPoint=point.map(([x,y])=&gt;{
       return {x:x,y:y}
   })

   console.log(newPoint);
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【ES6】展开运算符]]></title>
        <id>https://lonjinup.github.io/post/es6-zhan-kai-yun-suan-fu</id>
        <link href="https://lonjinup.github.io/post/es6-zhan-kai-yun-suan-fu">
        </link>
        <updated>2020-01-02T15:39:45.000Z</updated>
        <content type="html"><![CDATA[<h1 id="展开运算符">展开运算符</h1>
<h2 id="普通写法与展开运算符">普通写法与展开运算符</h2>
<ul>
<li>普通数组相加</li>
</ul>
<pre><code class="language-js">      function addNumbers(numbers) {
         return numbers.reduce((sum,number)=&gt;{
             return sum+number
         },0)
       };
      
      const  numbers=[1,2,3,4,5];
      console.log(addNumbers(numbers))  //15
</code></pre>
<ul>
<li>如果传入的是单个数字</li>
</ul>
<pre><code class="language-js">
    function addNumbers(a,b,c,d,e) {
        let numbers=[a,b,c,d,e];
        return numbers.reduce((sum,number)=&gt;{
            return sum+number
        },0)
    };

    console.log(addNumbers(1,2,3,4,5)) //15

</code></pre>
<ul>
<li>展开运算符</li>
</ul>
<pre><code class="language-js">
    function addNumbers(...numbers) {

        return numbers.reduce((sum,number)=&gt;{
            return sum+number
        },0)
    };

    console.log(addNumbers(1,2,3,4,5)) //15
</code></pre>
<h2 id="展开运算符的应用">展开运算符的应用</h2>
<ul>
<li>可替代数组push等操作</li>
</ul>
<pre><code class="language-js">
    var defaultColor=['red','green'];
    var favoriteColor=['orange','yellow'];

    defaultColor=defaultColor.concat(favoriteColor);

    console.log(defaultColor); //[&quot;red&quot;, &quot;green&quot;, &quot;orange&quot;, &quot;yellow&quot;]

    //同理也可以直接传两个数组
    console.log([...defaultColor,...favoriteColor])

    //之前的数组也可以合并

     var fallcolors=['fire red','fall orange'];
     
    console.log(['blue',...fallcolors,...defaultColor,...favoriteColor])

</code></pre>
<h2 id="小案例">小案例</h2>
<ul>
<li>判断某个值是否在某个数组中，如果没有，就添加进去</li>
</ul>
<pre><code class="language-js">
     function shopList(...items) {
         if(items.indexOf('milk')&lt;0){
             return ['milk',...items]
         }
      };
    
      console.log(shopList('orange','bread','eggs'))
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【ES6系列】函数参数默认值]]></title>
        <id>https://lonjinup.github.io/post/es6-xi-lie-han-shu-can-shu-mo-ren-zhi</id>
        <link href="https://lonjinup.github.io/post/es6-xi-lie-han-shu-can-shu-mo-ren-zhi">
        </link>
        <updated>2019-12-29T14:32:43.000Z</updated>
        <content type="html"><![CDATA[<h1 id="函数参数默认值">函数参数默认值</h1>
<ul>
<li>优点：优化代码</li>
</ul>
<h2 id="案例1">案例1</h2>
<ul>
<li>比如要写一个ajax方法，此时我定义了两个参数，一个url，一个methods，如果我只传递了url，此时是会报错的。</li>
</ul>
<pre><code class="language-js">
    function ajaxRequest(url,methods) {
    
    };

    console.log(ajaxRequest(&quot;google.com&quot;)) //undefined
</code></pre>
<ul>
<li>如果不想有报错，此时就得做一些处理。</li>
</ul>
<pre><code class="language-js">
    function ajaxRequest(url,methods) {
       if(!methods){
          methods='GET'
        }
        return methods
     };
   
       console.log(ajaxRequest(&quot;google.com&quot;))
       console.log(ajaxRequest(&quot;google.com&quot;,'post'))
</code></pre>
<ul>
<li>而在ES6中，则可以定义函数默认值,如果没有传，直接会使用默认值。</li>
</ul>
<pre><code class="language-js">
    function ajaxRequest(url,methods='GET') {
       return methods
    };

    console.log(ajaxRequest(&quot;google.com&quot;))
    console.log(ajaxRequest(&quot;google.com&quot;,'post'))
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【ES6系列】增强对象字面量]]></title>
        <id>https://lonjinup.github.io/post/es6-xi-lie-zeng-qiang-dui-xiang-zi-mian-liang</id>
        <link href="https://lonjinup.github.io/post/es6-xi-lie-zeng-qiang-dui-xiang-zi-mian-liang">
        </link>
        <updated>2019-12-29T14:10:01.000Z</updated>
        <content type="html"><![CDATA[<h1 id="增强对象字面量">增强对象字面量</h1>
<ul>
<li>
<p>解决的问题：缩减代码，使代码具有可读性。</p>
</li>
<li>
<p>直接看代码便可理解。</p>
</li>
</ul>
<h2 id="es5">ES5</h2>
<pre><code class="language-js">    //创建对象
    new Object();  //{}

    //创建数组
    new Array();  //[]

    function createBookSop(inventory) {
        return{
            inventory:inventory,
            inventoryValue:function () {
                return this.inventory.reduce((total,book)=&gt;total+book.price,0)
            },
            priceForName:function (title) {
                return this.inventory.find(book=&gt;book.title===title).price
            }
        }
    };

    const inventory=[
        {title:'vue',price:12},
        {title:'node',price:15},
    ];

    const bookSop=createBookSop(inventory)

    console.log(bookSop.inventoryValue())

    console.log(bookSop.priceForName('node'))

</code></pre>
<h2 id="es6">ES6</h2>
<pre><code class="language-js">    //创建对象
        new Object();  //{}
    
        //创建数组
        new Array();  //[]
    
        function createBookSop(inventory) {
            
            return{
                inventory,
                
                inventoryValue() {
                    return this.inventory.reduce((total,book)=&gt;total+book.price,0)
                },
                
                priceForName(title) {
                    return this.inventory.find(book=&gt;book.title===title).price
                }
            }
            
        };
        
        const inventory=[
            {title:'vue',price:12},
            {title:'node',price:15},
        ];
    
        
        const bookSop=createBookSop(inventory);
        
        console.log(bookSop.inventoryValue());
        
        console.log(bookSop.priceForName('node'));
</code></pre>
<h2 id="使用案例">使用案例</h2>
<pre><code class="language-js">
    const  url='https://www.baidu.com';
    const  data={color:'red'};

    //es5书写

    function saveFile(url,data) {
       $.ajax({
           method:&quot;post&quot;,
           url:url,
           data:data
       })
     };

    //es6书写
    
    function saveFile(url,data) {
       $.ajax({
           url,
           data,
           method:&quot;post&quot;,
       })
    };
        
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【ES6系列】箭头函数]]></title>
        <id>https://lonjinup.github.io/post/es6-zhong-jian-tou-han-shu</id>
        <link href="https://lonjinup.github.io/post/es6-zhong-jian-tou-han-shu">
        </link>
        <updated>2019-12-22T16:03:07.000Z</updated>
        <content type="html"><![CDATA[<h1 id="箭头函数">箭头函数</h1>
<ul>
<li>解决问题：<br>
1.缩减代码<br>
2.改变 <strong>this</strong> 指向</li>
</ul>
<h2 id="普通写法vs箭头函数vs">普通写法vs箭头函数vs</h2>
<pre><code class="language-js">    //普通写法
    const number=function(number){
        return number*2
    };

    //箭头函数
    const number2=(number)=&gt;{
        return number*2
    };

    console.log(number2(2)) //--&gt;4
</code></pre>
<h2 id="箭头函数升级版本">箭头函数升级版本</h2>
<ul>
<li>升级版本:必须写到一行，<strong>return</strong> 可以省略。</li>
</ul>
<pre><code class="language-js">     const number3=(number)=&gt; number*2;

     console.log(number2(2)) //--&gt;4
</code></pre>
<ul>
<li>还可以继续简化(单个参数量)</li>
</ul>
<pre><code class="language-js">     const number3=number=&gt; number*2;

     console.log(number2(2)) //--&gt;4

     //或者可以这样
    const number3=(number=&gt; number*2);
</code></pre>
<ul>
<li>多个参数：参数必须加括号</li>
</ul>
<pre><code class="language-js">    const number5=(number,number2)=&gt; number+number2

    console.log(number5(2,1)) //--&gt; 3
</code></pre>
<ul>
<li>多条语句：后面必须加括号</li>
</ul>
<pre><code class="language-js">    const number5=(number,number2)=&gt;{
        
        sum=number+number2;

        return sum

    }

    console.log(number5(2,1)) //--&gt; 3
</code></pre>
<h2 id="改变this指向">改变<strong>this</strong>指向</h2>
<pre><code class="language-js">
  // 改变this指向

    const team={

        name:['tom','bob'],
        teamName:'ES6',
        teamSummary:function(){
            
            return this.name.map(function (items) {

                //当前this已经没有明确指向了

                return `${items}属于${this.teamName}分类下`

            })
        }
    };

    console.log(team.teamSummary())//this.teamName显示undefined;

</code></pre>
<ul>
<li>如果想显示teamName</li>
</ul>
<pre><code class="language-javascript">    
    //方法1(提前声明一个this)

    const team={

        name:['tom','bob'],
        teamName:'ES6',
        teamSummary:function(){

            let that=this;//声明一个this
            
            return this.name.map(function (items) {

                return `${items}属于${that.teamName}分类下`

            })
        }
    };

    console.log(team.teamSummary())//正常显示

    //方法2（bind()方法）

    const team2={

        name:['tom','bob'],
        teamName:'ES6',
        teamSummary:function(){
            
            return this.name.map(function (items) {

                return `${items}属于${that.teamName}分类下`

            }.bind(this))//把this绑定给函数
        }
    };

    //方法3（箭头函数）

    const team={
    
            name:['tom','bob'],
    
            teamName:'ES6',
    
            teamSummary:function(){
    
                return this.name.map((items)=&gt; {
    
                    //当前this已经没有明确指向了,如果要明确，就得在上一个return之前先声明一个this；
    
                    return `${items}属于${this.teamName}分类下`
    
                })
            }
        };
        

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【ES6系列】模板字符串]]></title>
        <id>https://lonjinup.github.io/post/es6-zhong-mo-ban-zi-fu-chuan</id>
        <link href="https://lonjinup.github.io/post/es6-zhong-mo-ban-zi-fu-chuan">
        </link>
        <updated>2019-11-25T16:18:39.000Z</updated>
        <content type="html"><![CDATA[<h1 id="es6中模板字符串">ES6中模板字符串</h1>
<h2 id="es5中模板字符串拼接">ES5中模板字符串拼接</h2>
<ul>
<li>如果有换行就需要通过 <strong>+</strong> 号拼接，非常不利于书写。</li>
</ul>
<pre><code class="language-js"> let template='&lt;h1&gt;hellow world&lt;/h1&gt;'+
                '&lt;p&gt;这是es5语法&lt;/p&gt;'

document.getElementById(&quot;app&quot;).innerHTML = template;

</code></pre>
<h2 id="es6中模板字符串-2">ES6中模板字符串</h2>
<ul>
<li>首尾使用 <strong>` `</strong> 符号，中间内容可随意换行，书写方便。</li>
</ul>
<pre><code class="language-js">let template=`&lt;h1&gt;hellow world&lt;/h1&gt;
                &lt;p&gt;这是es6语法&lt;/p&gt;
                &lt;ul&gt;
                    &lt;li&gt;1&lt;/li&gt;
                &lt;/ul&gt;
            `

   document.getElementById(&quot;app&quot;).innerHTML = template;
</code></pre>
<ul>
<li>传递变量：${}</li>
</ul>
<pre><code class="language-js">    let name='LonJin';

    let template=`&lt;h1&gt;hellow ${name}&lt;/h1&gt;
                &lt;p&gt;这是es6语法&lt;/p&gt;
                &lt;ul&gt;
                    &lt;li&gt;1&lt;/li&gt;
                &lt;/ul&gt;
            `

   document.getElementById(&quot;app&quot;).innerHTML = template;
</code></pre>
<ul>
<li>执行方法：${}</li>
</ul>
<pre><code class="language-js">    function makeUppercase(word){
        return word.toUpperCase()
    }

    let name='LonJin';
    let template=`&lt;h1&gt;${makeUppercase(&quot;hello&quot;)} ${name}&lt;/h1&gt;
                &lt;p&gt;这是es6语法&lt;/p&gt;
                &lt;ul&gt;
                    &lt;li&gt;1&lt;/li&gt;
                &lt;/ul&gt;
            `

   document.getElementById(&quot;app&quot;).innerHTML = template;

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【ES6系列】let,const与var的区别]]></title>
        <id>https://lonjinup.github.io/post/letconst-yu-var-de-qu-bie</id>
        <link href="https://lonjinup.github.io/post/letconst-yu-var-de-qu-bie">
        </link>
        <updated>2019-11-25T15:59:01.000Z</updated>
        <content type="html"><![CDATA[<h1 id="varlet-const的区别">var,let ,const的区别</h1>
<h2 id="var">var</h2>
<ul>
<li>全局变量</li>
</ul>
<pre><code class="language-js">    function textVar(){

        var a=30;

        if(true){

            var a=50;

            console.log(a);//50
        };

        console.log(a);//50 找离他最近的a

    };
</code></pre>
<h2 id="let">let</h2>
<ul>
<li>有作用域的概念</li>
</ul>
<pre><code class="language-js"> function textLet(){

        let a=30;

        if(true){

            let a=50;//有作用域的概念

            console.log(a);//50 
        };

        console.log(a);//30 

    };

    textLet();
</code></pre>
<h2 id="const">const</h2>
<ul>
<li>一但申明，不可修改,他是一个常量。</li>
</ul>
<pre><code class="language-js">    const x=10;

    x=100;

    console.log(x);//Uncaught TypeError: Assignment to constant variable.

</code></pre>
<ul>
<li>定义数组</li>
</ul>
<pre><code class="language-js">    const colors=[];

    colors.push('red');

    colors.push('green');

    console.log(colors)

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【ES6系列】数组新增的方法]]></title>
        <id>https://lonjinup.github.io/post/es6-shu-zu-zhong-de-fang-fa</id>
        <link href="https://lonjinup.github.io/post/es6-shu-zu-zhong-de-fang-fa">
        </link>
        <updated>2019-09-18T16:24:02.000Z</updated>
        <content type="html"><![CDATA[<h1 id="数组方法">数组方法</h1>
<h2 id="数组方法-2">数组方法</h2>
<ul>
<li>数组方法有 <strong>forEach</strong>, <strong>map</strong> , <strong>filter</strong>, <strong>find</strong>, <strong>every</strong>, <strong>some</strong>, <strong>reduce</strong>.</li>
</ul>
<h3 id="foreach">forEach</h3>
<pre><code class="language-Js">    //ES5遍历
    var colors=['red','green','yellow'];

    for(var i=0;i&lt;colors.length;i++){
        console.log(colors[i])
    }

    //ES6遍历
    colors.forEach(function(color){
        console.log(color)
    })
</code></pre>
<ul>
<li>迭代器函数首先把数组每个值依次循环，直到最后一个。forEach是通过<strong>回调函数</strong>来提供参数的。回调函数也是一种闭包，所以每个回调函数都有自己的<strong>私有作用域</strong>，互不影响，内部变量不会及时释放。如果在闭包中<strong>return</strong>的话，也只是在当前函数中返回，而且在forEach中其他闭包函数中还是存在的，所以会出现<strong>return</strong>中是没办法结束循环的。</li>
</ul>
<pre><code class="language-js">    let arr=[1,2,3];
    arr.forEach(function(value,key,arr){
        console.log(value);  //--&gt;1,2,3
        //console.log(key);  //--&gt;0,1,2
        //console.log(arr); //--&gt;arr
    });
</code></pre>
<ul>
<li>
<p>可以看出他在函数中有三个参数，<strong>value</strong> , <strong>key</strong>, <strong>arr</strong>.</p>
</li>
<li>
<p>其中函数也可以写在外部</p>
</li>
</ul>
<pre><code class="language-js">    let arr=[1,2,3];
    function arr(value,key,arr){
         console.log(value);  //--&gt;1,2,3
        //console.log(key);  //--&gt;0,1,2
        //console.log(arr); //--&gt;arr
    }
    arr.forEach(arr)

</code></pre>
<h3 id="map">map</h3>
<ul>
<li>场景1 假定有一个数值数组(A),将A数组中的值以双倍的形式放到B数组中</li>
</ul>
<pre><code class="language-js">    var numbers=[1,2,3];
    var numbersA=[];

    // ES5
    for (var i=0; i&lt;numbers.length; i++){
        numbersA.push(numbers[i]*2)
    };
    console.log(numbersA);

    //ES6 map

    var numbersA=numbers.map(function(number){
        return number*2
    });

    console.log(numbersA);

    //forEach
    numbersA.forEach(function(number){
        console.log(number)
    })

</code></pre>
<ul>
<li>场景2 假定有一个对象数组（A），将A数组中对像某个属性的值存储到B数组中</li>
</ul>
<pre><code class="language-js">    var cars=[
        {model:&quot;buick&quot;,price:&quot;cheap&quot;},
        {model:&quot;bmw&quot;,price:&quot;expensive&quot;},
    ];

    var prices=cars.map(function(car){
        return cars.price
    });

    console.log(prices)
</code></pre>
<ul>
<li>原理：把数组中的值放入迭代器函数中，然后<strong>return</strong>结果出来，map需要返回值，如果不给<strong>return</strong>，默认返回的是undefined。map返回的是<strong>新数组</strong>。</li>
</ul>
<h3 id="filter">filter</h3>
<ul>
<li>场景1 假定有一个对象数组（A），获取数组中指定类型的对象放到B数组中</li>
</ul>
<pre><code class="language-js">   let products=[
       {name:'cucumber',type:'vegetable'},
       {name:'banana',type:'fruit'},
       {name:'celery',type:'vegetable'},
       {name:'orange',type:'fruit'},
    ];

    //ES5
    let filteredProducts=[];

    for (var i=0;i&lt;products.length;i++){

        if(products[i].type==&quot;fruit&quot;){

            filteredProducts.push(products[i])
        }
    };
    console.log(filteredProducts);

    //ES6

    let filter2=products.filter(function(product){

        return product.type==&quot;vegetable&quot;

    });
</code></pre>
<ul>
<li>
<p>原理：把数组中的值放入迭代器函数中，如果与匹配的内容相等，<strong>return</strong>true,把不匹配的过滤出去。</p>
</li>
<li>
<p>场景2 假定有一个对象数组A，过滤多个满足条件对象</p>
</li>
</ul>
<pre><code class="language-js">    let product2=[
        {name:'cucumber',type:'vegetable',price:5},
        {name:'banana',type:'fruit',price:5},
        {name:'celery',type:'vegetable',price:12},
        {name:'orange',type:'fruit',price:5},
    ];

    let newProduct=product2.filter(function(product){

        return product.type===&quot;vegetable&quot; &amp;&amp; product.price &gt; 0  &amp;&amp; product.price &lt; 10 

    });
    console.log(newProduct);
</code></pre>
<ul>
<li>场景3 假定有两个数组AB，根据A中id值，过滤掉B数组中不符合的数据。</li>
</ul>
<pre><code class="language-js"> let post={id:4,title:&quot;javascript&quot;};

    let comments=[
        {postId:4,content:&quot;vue&quot;},
        {postId:3,content:&quot;vue2.0&quot;},
        {postId:2,content:&quot;vue3.0&quot;},
        {postId:2,content:&quot;Node.js&quot;}
    ];

    function commentForPost(post,comments){

        return comments.filter(function(comment){
            return comment.postId==post.id
        });

    };

    console.log(commentForPost(post,comments));
</code></pre>
<h3 id="find">find</h3>
<ul>
<li>场景1 假定有一个对象数组A，找到符合条件的对象</li>
</ul>
<pre><code class="language-js">
    let users=[
        {name:&quot;jill&quot;},
        {name:&quot;tome&quot;},
        {name:&quot;bill&quot;}
    ];

    let user;

    //ES5
    for(var i=0;i&lt;users.length;i++){

        if(users[i].name==&quot;tome&quot;){

            user=users[i];
            break;//找到不会执行后面的
        }
    };

    //ES6

    user=users.find(function(user){

        return user.name==&quot;tome&quot;;
    })
    
</code></pre>
<ul>
<li>场景2 假定有一个对象数组A，根据指定对象的条件找到数组中符合条件的对象</li>
</ul>
<pre><code class="language-js">    let posts=[
            {id:3,title:&quot;node.js&quot;},
            {id:4,title:&quot;vue.js&quot;}
    ];

    let comment={id:3,content:&quot;hello word&quot;};

    function postForComment(posts,comment){

        return posts.find(function(post){

            return post.id==comment.id;

        })
    };
</code></pre>
<h3 id="every-一假即假-some-一真即真">every (一假即假 ) some (一真即真)</h3>
<ul>
<li>场景1 计算对象数组中每个电脑的操作系统是否可用，大于16位操作系统表示可用，否则不可用</li>
</ul>
<pre><code class="language-js">    let computers=[
        {name:&quot;Apple&quot;,ram:16},
        {name:&quot;IBM&quot;,ram:4},
        {name:&quot;Acer&quot;,ram:32},
    ];

    var everyComputers=true;

    var someComputers=false;

    //ES5

    for(var i=0;i&lt;computers.length;i++){

        var computer=computers[i];
        console.log(computer.ram)
        if(computer.ram &lt; 16){
          
            everyComputers=false
        }else{
            someComputers=true
        }
    };

    //ES6
   var every=computers.every(function(computer){

        return computer.ram&gt;16

    });

    console.log(every);

     var some=computers.some(function(computer){

        return computer.ram&gt;16

    });

    console.log(some);

</code></pre>
<ul>
<li>场景2 假定有一个注册页面，判断所有input内容长度是否大于0</li>
</ul>
<pre><code class="language-js">    function Filed(value){

        this.value=value;

    };
    
    Filed.prototype.validate=function(){

        return this.value.length&gt;0;
    }

    
    var username=new Filed(&quot;&quot;);

    var telephone=new Filed(&quot;18282828282828&quot;);

    var email=new Filed(&quot;LonJin_up@163.com&quot;);

    console.log(username.validate());

    console.log(telephone.validate());

    var fields=[username,telephone,email];

    var formIsValid=fields.every(function(field){

        return field.validate()
    });

    if(formIsValid){

        console.log(&quot;注册成功&quot;)

    }else{

        console.log(&quot;注册失败&quot;) 
    }
</code></pre>
<h3 id="reduce">reduce</h3>
<ul>
<li>场景1，计算数组中所有值的总和</li>
</ul>
<pre><code class="language-js"> //ES5
    var numbers=[10,20,30];

    var sum=0;

    for (var i=0; i &lt; numbers.length; i++){

        sum+=numbers[i];

    }

    console.log(sum);


    //ES6
    var sumValue=numbers.reduce(function(sum2,number2){

        console.log(sum2);//第一个参数需要初始化，所以打印出来第一个是0

        return sum2+number2

    },0);

    console.log(sumValue);
</code></pre>
<ul>
<li>场景2 将数组中对象的某个属性抽离到另外一个数组中</li>
</ul>
<pre><code class="language-js">    var primaryColor=[

        {color:'red'},
        {color:'yellow'},
        {color:'blue'},

    ];

    var color=primaryColor.reduce(function(previous,primaryColor){

        previous.push(primaryColor.color);

        return previous

    },[]);

    console.log(color)
</code></pre>
<ul>
<li>场景3 判断字符串中括号是否对称</li>
</ul>
<pre><code class="language-js">    function balanceParens(string){

        return !string.split(&quot;&quot;).reduce(function(previous,char){


            if(previous&lt;0){

                return previous

            }

            if (char==&quot;(&quot;){

                return ++previous

            }

            if (char==&quot;)&quot;){
                return --previous
            }

            return previous

        },0);

    }

    console.log(balanceParens(&quot;(((())))&quot;))
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[08-Vue中表单输入绑定]]></title>
        <id>https://lonjinup.github.io/post/08-vue</id>
        <link href="https://lonjinup.github.io/post/08-vue">
        </link>
        <updated>2019-08-04T17:04:33.000Z</updated>
        <content type="html"><![CDATA[<h1 id="表单输入绑定">表单输入绑定</h1>
<h2 id="基础用法-v-model">基础用法 <strong>v-model</strong></h2>
<ul>
<li>
<p><strong>v-model</strong> 指令在表单input、textarea及select元素上创建双向数据绑定。它会根据控件类型自动选取正确的方法来更新元素。</p>
</li>
<li>
<p>v-model会忽略掉所有表单元素的<strong>vaule checked selected</strong> 特性的初始值而总是将 Vue 实例的数据作为数据来源。你应该通过 JavaScript 在组件的 data 选项中声明初始值。</p>
</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app'&gt;
        &lt;input type=&quot;text&quot; v-model='message'&gt;
        &lt;span&gt;{{message}}&lt;/span&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
    var app=new Vue({
        el:&quot;#app&quot;,
        data:{
            message:''
        },
    })
&lt;/script&gt;
</code></pre>
<ul>
<li>单选绑定</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id=&quot;app2&quot;&gt;
        &lt;input type=&quot;checkbox&quot; v-model='checked'&gt;
        &lt;label for=&quot;checkbox&quot;&gt;{{ checked }}&lt;/label&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
    var app2=new Vue({
        el:&quot;#app2&quot;,
        data:{
            checked:true
        },
    })
&lt;/script&gt;
</code></pre>
<ul>
<li>多选</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app3'&gt;
        &lt;input type=&quot;checkbox&quot; name=&quot;&quot; id=&quot;西瓜&quot; value=&quot;西瓜&quot; v-model='checkedNames'&gt;
        &lt;label for=&quot;西瓜&quot;&gt;西瓜&lt;/label&gt;
        &lt;input type=&quot;checkbox&quot; name=&quot;&quot; id=&quot;香蕉&quot; value=&quot;香蕉&quot; v-model='checkedNames'&gt;
        &lt;label for=&quot;香蕉&quot;&gt;香蕉&lt;/label&gt;
        &lt;input type=&quot;checkbox&quot; name=&quot;&quot; id=&quot;水果&quot; value=&quot;水果&quot; v-model='checkedNames'&gt;
        &lt;label for=&quot;水果&quot;&gt;水果&lt;/label&gt;
        &lt;input type=&quot;checkbox&quot; name=&quot;&quot; id=&quot;喜欢&quot; value=&quot;喜欢&quot; v-model='checkedNames'&gt;
        &lt;label for=&quot;喜欢&quot;&gt;喜欢&lt;/label&gt;
        &lt;p&gt;{{checkedNames}}&lt;/p&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
    var app3=new Vue({
        el:&quot;#app3&quot;,
        data:{
            checkedNames: []
        }
    })
&lt;/script&gt;
</code></pre>
<ul>
<li>单选按钮</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app4'&gt;
        &lt;input type=&quot;radio&quot; id='one' value=&quot;one&quot; v-model='picked'&gt;
        &lt;label for=&quot;one&quot;&gt;one&lt;/label&gt;
        &lt;input type=&quot;radio&quot; id='two' value=&quot;two&quot; v-model='picked'&gt;
        &lt;label for=&quot;two&quot;&gt;two&lt;/label&gt;
        &lt;p&gt;{{picked}}&lt;/p&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
     var app4=new Vue({
        el:'#app4',
        data:{
            picked:''
        }
    })
&lt;/script&gt;
</code></pre>
<ul>
<li>选择框</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app4'&gt;
        &lt;input type=&quot;radio&quot; id='one' value=&quot;one&quot; v-model='picked'&gt;
        &lt;label for=&quot;one&quot;&gt;one&lt;/label&gt;
        &lt;input type=&quot;radio&quot; id='two' value=&quot;two&quot; v-model='picked'&gt;
        &lt;label for=&quot;two&quot;&gt;two&lt;/label&gt;
        &lt;p&gt;{{picked}}&lt;/p&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
     var app4=new Vue({
        el:'#app4',
        data:{
            picked:''
        }
    })
&lt;/script&gt;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[07-Vue中事件处理]]></title>
        <id>https://lonjinup.github.io/post/07-vue</id>
        <link href="https://lonjinup.github.io/post/07-vue">
        </link>
        <updated>2019-08-04T17:03:42.000Z</updated>
        <content type="html"><![CDATA[<h1 id="事件处理">事件处理</h1>
<h2 id="监听事件">监听事件</h2>
<ul>
<li>可以用<em>v-on</em>指令监听 DOM 事件，并在触发时运行一些 JavaScript 代码。</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app'&gt;
        &lt;div v-on:click='number+=1'&gt;click me&lt;/div&gt;
        &lt;div&gt;{{number}}&lt;/div&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
    var app=new Vue({
        el:&quot;#app&quot;,
        data:{
            number:1
        }
    })
&lt;/script&gt;
</code></pre>
<h2 id="事件处理方法">事件处理方法</h2>
<ul>
<li>然而许多事件处理逻辑会更为复杂，所以直接把 JavaScript 代码写在 v-on 指令中是不可行的。因此 v-on 还可以接收一个需要调用的方法名称。</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app2'&gt;
        &lt;div @click='alertFun' class=&quot;aaa&quot;&gt;click me&lt;/div&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
     var app2=new Vue({
        el:&quot;#app2&quot;,
        data:{
            hello:&quot;hello world&quot;,
        },
        methods:{
            alertFun:function(e){
                alert(this.hello+&quot;!&quot;);
                console.log(e);
                if(e){
                    alert(e.target.tagName)
                }
            }
        }
    })
&lt;/script&gt;
</code></pre>
<h2 id="内联处理器方法">内联处理器方法</h2>
<ul>
<li>除了直接绑定到一个方法，也可以在内联 JavaScript 语句中调用方法：</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
     &lt;div id='app3'&gt;
        &lt;div @click='alertMsg(&quot;hello world&quot;)'&gt;click me&lt;/div&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
    let app3=new Vue({
        el:&quot;#app3&quot;,
        methods:{
            alertMsg:function(msg){
                alert(msg)
            },
        }
    })
&lt;/script&gt;
</code></pre>
<h2 id="事件修饰符">事件修饰符</h2>
<ul>
<li>
<p>vue在<strong>v-on</strong>提供了事件修饰符</p>
<ul>
<li>.stop</li>
<li>.prevent</li>
<li>.capture</li>
<li>.self</li>
<li>.once</li>
<li>.passive</li>
<li>.once</li>
</ul>
</li>
</ul>
<pre><code class="language-html">
    &lt;!-- 阻止单击事件继续传播 --&gt;
    &lt;a v-on:click.stop=&quot;doThis&quot;&gt;&lt;/a&gt;

    &lt;!-- 提交事件不再重载页面 --&gt;
    &lt;form v-on:submit.prevent=&quot;onSubmit&quot;&gt;&lt;/form&gt;

    &lt;!-- 修饰符可以串联 --&gt;
    &lt;a v-on:click.stop.prevent=&quot;doThat&quot;&gt;&lt;/a&gt;

    &lt;!-- 只有修饰符 --&gt;
    &lt;form v-on:submit.prevent&gt;&lt;/form&gt;

    &lt;!-- 添加事件监听器时使用事件捕获模式 --&gt;
    &lt;!-- 即元素自身触发的事件先在此处理，然后才交由内部元素进行处理 --&gt;
    &lt;div v-on:click.capture=&quot;doThis&quot;&gt;...&lt;/div&gt;

    &lt;!-- 只当在 event.target 是当前元素自身时触发处理函数 --&gt;
    &lt;!-- 即事件不是从内部元素触发的 --&gt;
    &lt;div v-on:click.self=&quot;doThat&quot;&gt;...&lt;/div&gt;

    &lt;!-- 点击事件将只会触发一次 --&gt;
    &lt;a v-on:click.once=&quot;doThis&quot;&gt;&lt;/a&gt;

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[06-Vue中列表渲染]]></title>
        <id>https://lonjinup.github.io/post/06-vue</id>
        <link href="https://lonjinup.github.io/post/06-vue">
        </link>
        <updated>2019-08-04T17:02:52.000Z</updated>
        <content type="html"><![CDATA[<h1 id="列表渲染">列表渲染</h1>
<h2 id="数组渲染">数组渲染</h2>
<ul>
<li>我们用<strong>v-for</strong>指令根据一组数组的选项列表进行渲染。v-for 指令需要使用<strong>item in items</strong>形式的特殊语法items是源数据数组并且<strong>item</strong>是数组元素迭代的别名。</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
    &lt;div id='app'&gt;
        &lt;!-- 这里还可以用of来代替in --&gt;
        &lt;ul v-for='item in list' &gt;
            &lt;li :data-id='item.id'&gt;{{item.name}}&lt;/li&gt;
        &lt;/ul&gt;
    &lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
        var app=new Vue({
            el:&quot;#app&quot;,
            data:{
                list:[
                    {id:1,name:'hellow'},
                    {id:2,name:'world'},
                    {id:3,name:'hellow world'},
                ]
            }
        })
    &lt;/script&gt;
</code></pre>
<h2 id="对象渲染">对象渲染</h2>
<ul>
<li>你也可以用<strong>v-for</strong>通过一个对象的属性来迭代。</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app2'&gt;
            &lt;ul v-for='i in object'&gt;
                &lt;li&gt;{{i}}&lt;/li&gt;
            &lt;/ul&gt;
        &lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
         //对象渲染
        var app2=new Vue({
            el:&quot;#app2&quot;,
            data:{
                object:{
                    title:&quot;hellow&quot;,
                    name:&quot;world&quot;,
                    class:&quot;hellow world&quot;
                }
            }
        })
    &lt;/script&gt;
</code></pre>
<ul>
<li>如果需要键名可以这样写：</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app2'&gt;
            &lt;ul v-for='(name,value) in object'&gt;
                &lt;li&gt;{{name}}:{{value}}&lt;/li&gt;
            &lt;/ul&gt;
        &lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
         //对象渲染
        var app2=new Vue({
            el:&quot;#app2&quot;,
            data:{
                object:{
                    title:&quot;hellow&quot;,
                    name:&quot;world&quot;,
                    class:&quot;hellow world&quot;
                }
            }
        })
    &lt;/script&gt;
</code></pre>
<ul>
<li>还可以加索引:</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app2'&gt;
             &lt;ul v-for='(value,name,index) in object'&gt;
                &lt;li&gt;{{index}}.{{name}}：{{value}}&lt;/li&gt;
            &lt;/ul&gt;
        &lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
         //对象渲染
        var app2=new Vue({
            el:&quot;#app2&quot;,
            data:{
                object:{
                    title:&quot;hellow&quot;,
                    name:&quot;world&quot;,
                    class:&quot;hellow world&quot;
                }
            }
        })
    &lt;/script&gt;
</code></pre>
<ul>
<li>所以三个参数顺序为:<strong>值，键，索引</strong></li>
</ul>
<h2 id="数组更新检测">数组更新检测</h2>
<h3 id="变异方法">变异方法</h3>
<ul>
<li>
<p>Vue 包含一组观察数组的变异方法，所以它们也将会触发视图更新。这些方法如下(顺便复习一波数组的常用方法)：</p>
</li>
<li>
<p>push() <strong>添加一个或多个元素到数组的末尾，并返回数组新的长度</strong></p>
</li>
<li>
<p>pop()  <strong>删除一个数组中的最后一个元素，并且返回这个元素</strong></p>
</li>
<li>
<p>shift()  <strong>删除数组的第一个元素，并返回这个元素</strong></p>
</li>
<li>
<p>unshift() <strong>在数组的开头插入一个或多个元素，并返回数组的新长度</strong></p>
</li>
<li>
<p>splice()  <strong>在任意的位置给数组添加或删除任意个元素（拼接），返回被删除的元素组成的数组，没有则返回空数组</strong><br>
start: 开始操作的索引<br>
deleteCount：要移除的数组元素的个数<br>
itemN:要添加进数组的元素，如果不指定，则splice只删除数组元素</p>
</li>
<li>
<p>sort()  <strong>对数组的元素做原地的排序，并返回这个数组。sort可能不稳定，默认按照字符串的unicode码位点排序</strong></p>
</li>
<li>
<p>reverse()  <strong>前后颠倒数组中元素的位置，第一个元素会成为最后一个</strong></p>
</li>
<li>
<p>所以你可以在控制台输入<strong>app.list.push({id:1,name:&quot;ssss&quot;})</strong></p>
</li>
</ul>
<h4 id="替换数组">替换数组</h4>
<ul>
<li>
<p>变异方法 (mutation method)，顾名思义，会改变被这些方法调用的原始数组。相比之下，也有非变异 (non-mutating method) 方法，例如：filter(), concat() 和 slice() 。这些不会改变原始数组，但总是返回一个新数组。当使用非变异方法时，可以用新数组替换旧数组：</p>
<p>concat() <strong>先创建当前数组一个副本，然后将接收到的参数添加到这个副本的末尾，最后返回新构建的数组</strong><br>
slice()  <strong>基于当前数组中一个或多个项创建一个新数组，接受一个或两个参数，即要返回项的起始和结束位置，最后返回新数组</strong><br>
map()  <strong>对数组的每一项运行给定函数，返回每次函数调用的结果组成的数组</strong><br>
filter()  <strong>对数组中的每一项运行给定函数，该函数会返回true的项组成的数组</strong></p>
</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;!-- 替换数组 --&gt;
    &lt;div id='app3'&gt;
        &lt;button @click='concat'&gt;concat&lt;/button&gt;
        &lt;button @click='slice'&gt;slice&lt;/button&gt;
        &lt;button @click='map'&gt;map&lt;/button&gt;
        &lt;button @click='filter'&gt;filter&lt;/button&gt;
         &lt;ul v-for='item in items'&gt;
            &lt;li&gt;{{item}}&lt;/li&gt;
        &lt;/ul&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
    //替换数组
    var app3=new Vue({
        el:&quot;#app3&quot;,
        data:{
            items:['a','b','c','d']
        },
        methods:{
            concat(){
                this.items=this.items.concat('concat')
            },
            slice(){
                this.items=this.items.slice(1)
            },
            map(){
                this.items=this.items.map(function(item,index,arr){
                    return index+item
                })
            },
            filter(){
                this.items=this.items.filter(function(item,index,arr){
                    return (index&gt;1)
                })
            }
        }
    })
&lt;/script&gt;
</code></pre>
<h2 id="注意事项">注意事项</h2>
<ul>
<li>由于JavaScript的限制，Vue不能检测以下变动的数组：</li>
</ul>
<pre><code class="language-JavaScript">    var app=new Vue({
        el:&quot;#app&quot;,
        data:{
            items:['a','b','c'];
        }
    });
    app.items[1]='hellow';//不响应
    app.items.length = 2 // 不响应
</code></pre>
<h3 id="解决方法">解决方法</h3>
<ul>
<li>
<p>Vue.set();</p>
</li>
<li>
<p><strong>Vue.set(vm.items, indexOfItem, newValue)</strong></p>
</li>
<li>
<p>Array.prototype.splice</p>
</li>
<li>
<p><strong>vm.items.splice(indexOfItem, 1, newValue)</strong></p>
</li>
</ul>
<pre><code class="language-JavaScript">    var app=new Vue({
        el:&quot;#app&quot;,
        data:{
            items:['a','b','c'];
        }
    });
</code></pre>
<ul>
<li>你也可以使用<strong>vm.$set</strong>实例方法，该方法是全局方法<strong>Vue.set</strong>的一个别名：</li>
</ul>
<pre><code class="language-JavaScript">    vm.$set(vm.items, indexOfItem, newValue)
</code></pre>
<ul>
<li>为了解决第二类问题，你可以使用splice</li>
</ul>
<pre><code class="language-JavaScript">    vm.items.splice(newLength)
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[05-Vue中条件渲染]]></title>
        <id>https://lonjinup.github.io/post/05-vue</id>
        <link href="https://lonjinup.github.io/post/05-vue">
        </link>
        <updated>2019-08-04T17:02:05.000Z</updated>
        <content type="html"><![CDATA[<h1 id="条件渲染">条件渲染</h1>
<h2 id="条件渲染-2">条件渲染</h2>
<h3 id="v-if-v-else-if-v-else">v-if v-else-if v-else</h3>
<ul>
<li>在<strong>template</strong>元素上使用 v-if 条件渲染分组：因为 v-if 是一个指令，所以必须将它添加到一个元素上。但是如果想切换多个元素呢？此时可以把一个<strong>template</strong>元素当做不可见的包裹元素，并在上面使用 v-if。最终的渲染结果将不包含<strong>template</strong>元素。</li>
</ul>
<pre><code class="language-html">&lt;template v-if=&quot;ok&quot;&gt;
  &lt;h1&gt;Title&lt;/h1&gt;
  &lt;p&gt;Paragraph 1&lt;/p&gt;
  &lt;p&gt;Paragraph 2&lt;/p&gt;
&lt;/template&gt;
</code></pre>
<ul>
<li>Vue中还提供了<strong>v-lese</strong>和<strong>else-if</strong>,可以连续使用。</li>
</ul>
<pre><code class="language-html">    &lt;div v-if=&quot;type === 'A'&quot;&gt;
    A
    &lt;/div&gt;
    &lt;div v-else-if=&quot;type === 'B'&quot;&gt;
    B
    &lt;/div&gt;
    &lt;div v-else-if=&quot;type === 'C'&quot;&gt;
    C
    &lt;/div&gt;
    &lt;div v-else&gt;
    Not A/B/C
    &lt;/div&gt;
</code></pre>
<h4 id="用key管理可复用的元素">用<strong>key</strong>管理可复用的元素</h4>
<ul>
<li>Vue 会尽可能高效地渲染元素，通常会复用已有元素而不是从头开始渲染。这么做除了使 Vue 变得非常快之外，还有其它一些好处。例如，如果你允许用户在不同的登录方式之间切换：</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app'&gt;
        &lt;template v-if='ok'&gt;
           &lt;span&gt;a&lt;/span&gt;
           &lt;input type=&quot;text&quot; placeholder=&quot;ss&quot;&gt;
        &lt;/template&gt;
        &lt;template v-else&gt;
            &lt;span&gt;d&lt;/span&gt;
            &lt;input type=&quot;text&quot; placeholder=&quot;ss&quot;&gt;
        &lt;/template&gt;
        &lt;div @click='choose'&gt;切换&lt;/div&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src='vue.js'&gt;&lt;/script&gt;
&lt;script&gt;
    var app=new Vue({
        el:&quot;#app&quot;,
        data:{
            ok:false
        },
        methods:{
            choose:function(){
               if(app.ok==true){
                  this.ok=false;
               }else if(app.ok==false){
                  this.ok=true;
               }
            }
        }
    })
&lt;/script&gt;
</code></pre>
<ul>
<li>在上面示例中切换input，不会清除输入内容，因为两个模板用了相同的内容， input不会被替换掉，仅仅替换了他的placeholder。如果不要复用他们，只需要给input上加一个<strong>key</strong>属性</li>
</ul>
<pre><code class="language-html">    &lt;div id='app'&gt;
        &lt;template v-if='ok'&gt;
           &lt;span&gt;a&lt;/span&gt;
           &lt;input type=&quot;text&quot; placeholder=&quot;ss&quot; key='hellow'&gt;
        &lt;/template&gt;
        &lt;template v-else&gt;
            &lt;span&gt;d&lt;/span&gt;
            &lt;input type=&quot;text&quot; placeholder=&quot;ss&quot; key='你好'&gt;
        &lt;/template&gt;
        &lt;div @click='choose'&gt;切换&lt;/div&gt;
    &lt;/div&gt;
</code></pre>
<h3 id="v-show">v-show</h3>
<ul>
<li>v-show与v-if不同的是带有 v-show 的元素始终会被渲染并保留在 DOM 中。v-show 只是简单地切换元素的 CSS 属性 display。<strong>v-show不支持template元素</strong>也不支持<strong>v-else</strong></li>
</ul>
<h3 id="v-show与v-if">v-show与v-if</h3>
<ul>
<li>
<p>v-if 是<strong>真正</strong>的条件渲染，因为它会确保在切换过程中条件块内的事件监听器和子组件适当地被销毁和重建。</p>
</li>
<li>
<p>v-if 也是惰性的：如果在初始渲染时条件为假，则什么也不做——直到条件第一次变为真时，才会开始渲染条件块。</p>
</li>
<li>
<p>相比之下，v-show 就简单得多——不管初始条件是什么，元素总是会被渲染，并且只是简单地基于 CSS 进行切换。</p>
</li>
<li>
<p>一般来说，v-if 有更高的<strong>切换开销</strong>，而 v-show 有更高的<strong>初始渲染开销</strong>。因此，如果需要非常频繁地切换，则使用 v-show 较好；如果在运行时条件很少改变，则使用 v-if 较好。</p>
</li>
</ul>
<h4 id="v-show-与v-if一起使用">v-show 与v-if一起使用</h4>
<ul>
<li>
<p>不推荐同时使用 v-if 和 v-for。请查阅风格指南<a href="https://cn.vuejs.org/v2/style-guide/#%E9%81%BF%E5%85%8D-v-if-%E5%92%8C-v-for-%E7%94%A8%E5%9C%A8%E4%B8%80%E8%B5%B7-%E5%BF%85%E8%A6%81">风格指南</a>以获取更多信息。</p>
</li>
<li>
<p>当v-if与v-for一起使用时，v-for具有比v-if更高的优先级。请查阅<a href="https://cn.vuejs.org/v2/guide/list.html#v-for-with-v-if">列表渲染指南</a>以获取详细信息。</p>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[04-Vue中class和style绑定]]></title>
        <id>https://lonjinup.github.io/post/04-vue</id>
        <link href="https://lonjinup.github.io/post/04-vue">
        </link>
        <updated>2019-08-04T17:00:49.000Z</updated>
        <content type="html"><![CDATA[<h1 id="class-与-style-绑定">Class 与 Style 绑定</h1>
<ul>
<li>操作元素的 class 列表和内联样式是数据绑定的一个常见需求。因为它们都是属性，所以我们可以用<strong>v-bind</strong>处理它们：只需要通过表达式计算出字符串结果即可。不过，字符串拼接麻烦且易错。因此，在将 v-bind 用于 class 和 style 时，Vue.js 做了专门的增强。表达式结果的类型除了字符串之外，还可以是对象或数组。</li>
</ul>
<h2 id="绑定class">绑定class</h2>
<h3 id="对象语法">对象语法</h3>
<ul>
<li>我们可以给<strong>v-bind:class</strong>一个对象，动态切换class</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app' v-bind:class=&quot;{active:isActive}&quot;&gt;vue&lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
        var app=new Vue({
            el:&quot;#app&quot;,
            data:{
                isActive:true
            }
        })
    &lt;/script&gt;
</code></pre>
<ul>
<li>你可以在对象中传入更多属性来动态切换多个 class。此外，v-bind:class 指令也可以与普通的 class 属性共存。</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app' v-bind:class=&quot;{active:isActive,active2:isFalse}&quot; class=&quot;name&quot;&gt;dddd&lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
        var app=new Vue({
            el:&quot;#app&quot;,
            data:{
                isActive:true,
                isFalse:true
            }
        })
    &lt;/script&gt;
</code></pre>
<ul>
<li>绑定的数据对象不必内联定义在模板里</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app' v-bind:class=&quot;classObject&quot; class=&quot;name&quot;&gt;dddd&lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
        var app=new Vue({
            el:&quot;#app&quot;,
            data:{
                classObject: {
                    active: true,
                    'text-danger':false
                }
            }
        })
    &lt;/script&gt;
</code></pre>
<ul>
<li>还可以绑定到一个计算属性中</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app2' v-bind:class='classObject'&gt;222&lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
        let app2=new Vue({
            el:&quot;#app2&quot;,
            data:{
                isActive: true,
                error: null
            },
            computed:{
                classObject:function(){
                    return {
                        active:this.isActive &amp;&amp; !this.error,
                        'text-danger':this.error &amp;&amp; this.error.type === 'fatal'
                    }
                }
            },
        })
    &lt;/script&gt;
</code></pre>
<h3 id="数组语法">数组语法</h3>
<ul>
<li>我们可以把一个数组传给 v-bind:class，以应用一个 class 列表：</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app3' v-bind:class='[activeClass,errorClass]'&gt;&lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
         //数组语法
        var app3=new Vue({
            el:&quot;#app3&quot;,
            data:{
                activeClass: 'active',
                errorClass: 'text-danger'
            }
        })
    &lt;/script&gt;
</code></pre>
<ul>
<li>如果想通过条件切换列表中的class，可以用三元表达式:</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app4' v-bind:class='[isActive?activeClass:&quot;&quot;, errorClass]'&gt;&lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
        var app4=new Vue({
            el:&quot;#app4&quot;,
            data:{
                activeClass: 'active',
                errorClass: 'text-danger',
                isActive:false
            }
        });
    &lt;/script&gt;
</code></pre>
<ul>
<li>如果这样写觉得比较繁琐，也可以使用对象语法</li>
</ul>
<pre><code class="language-html">    &lt;body&gt;
        &lt;div id='app4' v-bind:class='[{isActive:activeClass}, errorClass]'&gt;&lt;/div&gt;
    &lt;/body&gt;
    &lt;script src='vue.js'&gt;&lt;/script&gt;
    &lt;script&gt;
        var app4=new Vue({
            el:&quot;#app4&quot;,
            data:{
                activeClass: 'active',
                errorClass: 'text-danger',
                isActive:false
            }
        });
    &lt;/script&gt;
</code></pre>
<h3 id="在组件中">在组件中</h3>
<ul>
<li>
<p>当在一个自定义组件上使用 class 属性时，这些类将被添加到该组件的根元素上面。这个元素上已经存在的类不会被覆盖。</p>
</li>
<li>
<p>如果你申明一个组件</p>
</li>
</ul>
<pre><code class="language-JavaScript">    Vue.component('my-component', {
      template: '&lt;p class=&quot;foo bar&quot;&gt;Hi&lt;/p&gt;'
    })
</code></pre>
<ul>
<li>然后使用组件时候再添加一些样式</li>
</ul>
<pre><code class="language-html">    &lt;my-component class='box color'&gt;&lt;/my-component&gt;
</code></pre>
<ul>
<li>这时候渲染出来的结果就是</li>
</ul>
<pre><code class="language-html">    &lt;my-component class='box color foo bar'&gt;&lt;/my-component&gt;
</code></pre>
<ul>
<li>对于绑定数据的class也一样</li>
</ul>
<pre><code class="language-html">    &lt;my-component v-bind:class='{active:isActive}'&gt;&lt;/my-component&gt;
</code></pre>
<ul>
<li>当<strong>isActive</strong>为<strong>true</strong>的时候：</li>
</ul>
<pre><code class="language-html">    &lt;my-component class='active foo bar'&gt;&lt;/my-component&gt;
</code></pre>
<h2 id="绑定内联样式">绑定内联样式</h2>
<ul>
<li><strong>v-bind:style</strong>的对象语法十分直观——看着非常像 CSS，但其实是一个 JavaScript 对象。CSS 属性名可以用驼峰式 (camelCase) 或短横线分隔 (font-size，记得用<strong>引号</strong>括起来) 来命名</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;!-- 横线 --&gt;
    &lt;div id='app5' v-bind:style=&quot;{ color: activeColor, 'font-size': fontSize + 'px' }&quot;&gt;&lt;/div&gt;
    &lt;!-- 驼峰 --&gt;
    &lt;div id='app5' v-bind:style=&quot;{ color: activeColor, fontSize: fontSize + 'px' }&quot;&gt;&lt;/div&gt;
&lt;/body&gt;
&lt;script&gt;
    var app5=new Vue({
        el:&quot;#app5&quot;,
        data: {
            activeColor: 'red',
            fontSize: 30
        }
    });
&lt;/script&gt;
</code></pre>
<h3 id="对象语法-2">对象语法</h3>
<ul>
<li>如果样式较多，可以绑定到一个对象上</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app6' v-bind:style='styleObj'&gt;&lt;/div&gt;
&lt;/body&gt;
&lt;script&gt;
    var app6=new Vue({
        el:&quot;#app5&quot;,
        data: {
           styleObj:{
              color:'green',
              //驼峰
              fontSize:20+'px',
              //横杠
              // 'font-size':20+'px'
            }
        }
    });
&lt;/script&gt;
</code></pre>
<h3 id="数组语法-2">数组语法</h3>
<ul>
<li>数组语法可以将多个样式对象应用到同一个元素上</li>
</ul>
<pre><code class="language-html">&lt;body&gt;
    &lt;div id='app7' v-bind:style=&quot;[styleObj,styleObj2]&quot;&gt;&lt;/div&gt;
&lt;/body&gt;
&lt;script&gt;
    //数组语法
        var app7=new Vue({
            el:&quot;#app7&quot;,
            data:{
                styleObj:{
                    color:'green',
                    //驼峰
                    fontSize:20+'px',
                    //横杠
                    // 'font-size':20+'px'
                },
                styleObj2:{
                    border:'none',
                },
            }
        });
&lt;/script&gt;
</code></pre>
<h3 id="自动添加前缀">自动添加前缀</h3>
<ul>
<li>当<strong>v-bind:style</strong>使用需要添加浏览器引擎前缀的 CSS 属性时，如transform，Vue.js 会自动侦测并添加相应的前缀。</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[03-Vue计算属性]]></title>
        <id>https://lonjinup.github.io/post/03-vue</id>
        <link href="https://lonjinup.github.io/post/03-vue">
        </link>
        <updated>2019-08-04T16:59:53.000Z</updated>
        <content type="html"><![CDATA[<h1 id="计算属性和侦听器">计算属性和侦听器</h1>
<h2 id="概念">概念</h2>
<ul>
<li>模板内的表达式非常便利，但是设计它们的初衷是用于简单运算的。在模板中放入太多的逻辑会让模板过重且难以维护。例如</li>
</ul>
<pre><code class="language-Html">  &lt;div id=&quot;example&quot;&gt;
    {{ message.split('').reverse().join('') }}
  &lt;/div&gt;
</code></pre>
<ul>
<li>
<p>在这个地方，模板不再是简单的声明式逻辑。你必须看一段时间才能意识到，这里是想要显示变量 message 的翻转字符串。当你想要在模板中多次引用此处的翻转字符串时，就会更加难以处理。所以对于复杂的逻辑，就可以使用计算属性。</p>
</li>
<li>
<p>简单的说:<br>
1.计算属性其实就是 Vue 实例的一个属性<br>
2.计算属性一般依赖传统的 Vue 实例属性<br>
3.计算属性一般是通过运算得到的属性</p>
</li>
</ul>
<pre><code class="language-Html">
  &lt;body&gt;
      &lt;div id=&quot;app&quot;&gt;{{reversedMessage}}&lt;/div&gt;
  &lt;/body&gt;
  &lt;script src='vue.js'&gt;&lt;/script&gt;
  &lt;script&gt;
      var app=new Vue({
          el:&quot;#app&quot;,
          data:{
              msg:&quot;hellow world&quot;
          },
          //计算属性简单写法
          computed:{
              reversedMessage(){
                  return this.msg.split('').reverse().join('')
              }
              //完整写法
              // reversedMessage: {
              //     get() { //获取值
              //     return this.msg.split('').reverse().join('')
              //     },
              //     set(val) { // set方法：设置值
              //     this.msg = val.split('').reverse().join('')
              //     }
              // }
          },
      })
  &lt;/script&gt;

</code></pre>
<ul>
<li>方法</li>
</ul>
<pre><code class="language-Html">
  &lt;body&gt;
      &lt;div id=&quot;app&quot;&gt;{{reversedMessage()}}&lt;/div&gt;
  &lt;/body&gt;
  &lt;script src='vue.js'&gt;&lt;/script&gt;
  &lt;script&gt;
      var app=new Vue({
          el:&quot;#app&quot;,
          data:{
              msg:&quot;hellow world&quot;
          },
          //方法
          methods:{
              reversedMessage:function(){
                  return this.msg.split('').reverse().join('')
              }
          }
      })
  &lt;/script&gt;

</code></pre>
<ul>
<li>
<p>计算属性是基于它们的依赖进行缓存的，只有在它<strong>相关的依赖发生改变时才会重新求值</strong>，即计算属性会对计算出来的结果进行缓存，这就意味着只要 <strong>message</strong>还没有发生改变，多次访问<strong>reversedMessage</strong>计算属性会立即返回之前的计算结果，而不必再次执行函数方法是每次重新渲染时，调用方法将总会再次执行函数，开销比较大</p>
</li>
<li>
<p>假设我们有一个性能开销比较大的计算属性A它需要遍历一个巨大的数组并做大量的计算。然后我们可能有其他的计算属性依赖于A.如果没有缓存,我们将不可避免的多次执行A的getter！如果你不希望有缓存，请用方法来替代</p>
</li>
</ul>
<h3 id="计算属性vs侦听属性">计算属性vs侦听属性</h3>
<ul>
<li>Vue 提供了一种更通用的方式来观察和响应 Vue 实例上的数据变动:<strong>侦听属性(watch)</strong></li>
</ul>
<pre><code class="language-html">  &lt;body&gt;
      &lt;!-- 侦听器 --&gt;
      &lt;div id='app2'&gt;
          &lt;p&gt;FullName: {{fullName}}&lt;/p&gt;
          &lt;p&gt;FirstName: &lt;input type=&quot;text&quot; v-model=&quot;firstName&quot;&gt;&lt;/p&gt;
      &lt;/div&gt;
  &lt;/body&gt;
  &lt;script src='vue.js'&gt;&lt;/script&gt;
  &lt;script&gt;
      var app2=new Vue({
          el: '#app2',
          data: {
              firstName: 'first',
              lastName: 'last',
              fullName: ''
          },
          watch: {
              firstName(newName, oldName) {
                  this.fullName = newName + ' ' + this.lastName;
              }
          }
      })
  &lt;/script&gt;
</code></pre>
<ul>
<li>watch 中的对象在 data 中已经定义了,当我们输入<strong>firstName</strong>后， watch监听每次修改变化的新值，然后计算输出fullName。也就是上面的代码中，<strong>fullName</strong> 一开始被渲染出来的时候是空值。</li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://s2.ax1x.com/2019/05/08/EyjCAU.png" alt="watch" loading="lazy"></figure>
<h4 id="watch的高级用法">watch的高级用法</h4>
<ul>
<li>handler方法和immediate属性:如上所述，一开始被渲染出来的时候，fullName是空值,如果想要一开始就让最初绑定的值执行该怎么办？我们只需要给firstName绑定一个<strong>handler</strong>方法，之前我们写的watch方法其实默认写的就是这个handler，Vue.js会去处理这个逻辑，最终编译出来其实就只这个handler，设置<strong>immediate:true</strong>代表如果在 wacth 里声明了<strong>firstName</strong>之后，就会立即先去执行里面的<strong>handler</strong>方法，如果为<strong>false</strong>就跟我们以前的效果一样，不会在绑定的时候就执行.</li>
</ul>
<pre><code class="language-html">
  &lt;body&gt;
      &lt;!-- 侦听器 --&gt;
      &lt;div id='app2'&gt;
          &lt;p&gt;FullName: {{fullName}}&lt;/p&gt;
          &lt;p&gt;FirstName: &lt;input type=&quot;text&quot; v-model=&quot;firstName&quot;&gt;&lt;/p&gt;
      &lt;/div&gt;
  &lt;/body&gt;
  &lt;script src='vue.js'&gt;&lt;/script&gt;
  &lt;script&gt;
    var app2=new Vue({
        el: '#app2',
        data: {
            firstName: 'first',
            lastName: 'last',
            fullName: ''
        },
        watch: {
            firstName:{
                handler(NewNmae,OldName){
                    this.fullName = NewNmae + ' ' + this.lastName;
                },
                 // 代表在wacth里声明了firstName这个方法之后立即先去执行handler方法
                 immediate: true
            }
        }
    })
  &lt;/script&gt;

</code></pre>
<ul>
<li>
<p>结果如下<br>
<img src="https://s2.ax1x.com/2019/05/08/Eyv9KI.png" alt="watch" loading="lazy"></p>
</li>
<li>
<p>deep属性默认值为false,代表是否深度监听</p>
</li>
</ul>
<h4 id="注销watch">注销watch</h4>
<ul>
<li>为什么要注销<strong>watch?<strong>因为我们的组件是经常要被销毁的，比如我们跳一个路由，从一个页面跳到另外一个页面，那么原来的页面的</strong>watch</strong>其实就没用了，这时候我们应该注销掉原来页面的<strong>watch</strong>的，不然的话可能会导致内置溢出。好在我们平时<strong>watch</strong>都是写在组件的选项中的，他会随着组件的销毁而销毁。</li>
</ul>
<pre><code class="language-JavaScript">  const app = new Vue({
      template: '&lt;div id=&quot;root&quot;&gt;{{text}}&lt;/div&gt;',
      data: {
        text: 0
      },
      watch: {
        text(newVal, oldVal){
          console.log(`${newVal} : ${oldVal}`);
        }
      }
  });
</code></pre>
<ul>
<li>但是，如果我们使用下面这样的方式写 watch，那么就要手动注销了，这种注销其实也很简单</li>
</ul>
<pre><code class="language-JavaScript">  const unWatch = app.$watch('text', (newVal, oldVal) =&gt; {
  console.log(`${newVal} : ${oldVal}`);
  })
  unWatch(); // 手动注销watch
</code></pre>
<ul>
<li><strong>app.$watch</strong>调用后会返回一个值，就是unWatch方法，你要注销<strong>watch</strong>只要调用<strong>unWatch</strong>方法就可以了。</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[02-Vue组件]]></title>
        <id>https://lonjinup.github.io/post/02-vue</id>
        <link href="https://lonjinup.github.io/post/02-vue">
        </link>
        <updated>2019-08-04T16:58:46.000Z</updated>
        <content type="html"><![CDATA[<h1 id="组件">组件</h1>
<ul>
<li>组件系统是 Vue 的另一个重要概念，因为它是一种抽象，允许我们使用小型、独立和通常可复用的组件构建大型应用。我们所写的页面都可以分割成一个个组件，比如头部，轮播，尾部等...</li>
</ul>
<h2 id="创建组件">创建组件</h2>
<ul>
<li>在Vue中，创建组件就是预定义了一个Vue的<strong>实例</strong>。</li>
</ul>
<pre><code class="language-Html">    &lt;script&gt;
    //创建一个名字为todo-item的组件
    Vue.component('todo-item',{
        template:&quot;&lt;li&gt;这是一个实例&lt;/li&gt;&quot;
    });
    &lt;/script&gt;
</code></pre>
<ul>
<li>如果这样使用只能渲染相同文本，所以可以通过定义一个<strong>prop</strong>可以接收从父组件传过来的数据</li>
</ul>
<pre><code class="language-Html">    &lt;div id=&quot;app&quot;&gt;
            &lt;ol&gt;
                &lt;todo-item v-for='item in list' v-bind:todo='item' v-bind:key='item.id'&gt;

                &lt;/todo-item&gt;
            &lt;/ol&gt;
    &lt;/div&gt;
    &lt;script&gt;
        //定义组件
        Vue.component('todo-item',{
            props:['todo'],
            template:&quot;&lt;li&gt;{{todo.text}}&lt;/li&gt;&quot;
        });
        var app=new Vue({
            el:&quot;#app&quot;,
            data:{
                list:[
                    {id:1,text:&quot;hellow world&quot;},
                    {id:2,text:&quot;hellow&quot;},
                    {id:3,text:&quot;hi&quot;},
                    {id:4,text:&quot;world&quot;},
                ]
            }
        })
    &lt;/script&gt;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[01-Vue基础知识]]></title>
        <id>https://lonjinup.github.io/post/01-vue</id>
        <link href="https://lonjinup.github.io/post/01-vue">
        </link>
        <updated>2019-08-04T16:50:18.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vue">Vue</h1>
<p><em>虽然年后就说开始学vue了，但一直断断续续的，得系统性学习一波了，笔记也顺便记录一下，方便查阅。</em></p>
<p><em>基础笔记基本来源于<a href="https://cn.vuejs.org/">官方文档</a>。</em></p>
<h2 id="vue起步">vue起步</h2>
<ul>
<li>初学可以先用script引入，先过一遍官方文档</li>
</ul>
<pre><code class="language-Html">&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
</code></pre>
<h3 id="模板语法">模板语法</h3>
<ul>
<li>现在开始，创建一个vue实例。</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
   &lt;div id=&quot;app&quot;&gt;{{message}}&lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
    var app=new Vue({
        el:&quot;#app&quot;, //指定绑定的目标dom元素的Id
        //绑定数据源
        data:{
            message:&quot;Hello World By Vue&quot;
        }
    });
&lt;/script&gt;
</code></pre>
<ul>
<li>
<p>这样，一个vue应用已经创建完成，可以看到，首选<strong>new</strong>了一个vue对象，然后通过<strong>el</strong>去绑定元素，通过在data定义一个值，然后通过类似模板语法的方式，输出到页面。此时页面属于响应式的，可以通过在控制台修改msg来看是否改变(<strong>app.message=&quot;hello World&quot;</strong>)。</p>
</li>
<li>
<p>很多场景下不需要去根据数据改变而更新页面数据，此时可以用<strong>v-once</strong></p>
</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
   &lt;div id=&quot;app&quot; v-once&gt;这个数据不会变{{message}}&lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
    var app=new Vue({
        el:&quot;#app&quot;, //指定绑定的目标dom元素的Id
        //绑定数据源
        data:{
            message:&quot;Hello World By Vue&quot;
        }
    });
&lt;/script&gt;
</code></pre>
<h2 id="指令">指令</h2>
<h3 id="v-html">v-html</h3>
<ul>
<li>双大括号会将数据解释为普通文本，而非 HTML 代码。为了输出真正的 HTML，你需要使用 <strong>v-html</strong> 指令：</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
   &lt;div id=&quot;app&quot;&gt;
       &lt;div v-html='rawhtml'&gt;&lt;/div&gt;
   &lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
    var app=new Vue({
        el:&quot;#app&quot;, //指定绑定的目标dom元素的Id
        //绑定数据源
        data:{
            rawhtml:&quot;&lt;p&gt;我要输出html&lt;p&gt;&quot;
        }
    });
&lt;/script&gt;
</code></pre>
<h3 id="v-bind">v-bind</h3>
<ul>
<li>还可以通过<strong>v-bind</strong>绑定元素特性,<strong>v-bind</strong>在vue中称为指令。</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
    &lt;div id=&quot;app2&quot; v-bind:title=&quot;message&quot;&gt;鼠标放在这试一试&lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
    var app=new Vue({
        el:&quot;#app2&quot;, //指定绑定的目标dom元素的Id
        //绑定数据源
        data:{
            message:'页面加载于'+new Date().toLocaleDateString(),
        }
    });
&lt;/script&gt;  
</code></pre>
<ul>
<li>Vue中还可以把bind指令<strong>缩写</strong></li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
    &lt;!-- 完整语法 --&gt;
    &lt;div id=&quot;app2&quot; v-bind:title=&quot;message&quot;&gt;鼠标放在这试一试&lt;/div&gt;
    &lt;!
    -- 缩写 --&gt;
    &lt;div id=&quot;app2&quot; :title=&quot;message&quot;&gt;鼠标放在这试一试&lt;/div&gt;
&lt;/body&gt;
</code></pre>
<h3 id="v-if-与-v-else-v-else-if">v-if 与 v-else v-else-if</h3>
<ul>
<li>很多场景下需要控制标签的显示隐藏，可以通过Vue中提供的<strong>v-if</strong>.</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
    &lt;div id=&quot;app3&quot;&gt;
        &lt;p v-if=&quot;show&quot;&gt;你现在可以看到我&lt;/p&gt;
        &lt;p v-else&gt;这是else&lt;/p&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
    var app3=new Vue({
        el:&quot;#app3&quot;, //指定绑定的目标dom元素的Id
        //绑定数据源
        data:{
            show:true
        }
    });
&lt;/script&gt;  
</code></pre>
<ul>
<li>此时<strong>data</strong>中<strong>show:true</strong>是显示的，我们在控制台或者代码中改成<strong>show:false</strong>,可以看到，标签不显示了。</li>
<li><strong>v-else</strong>必须紧跟在<strong>v-if</strong>或者<strong>v-else-if</strong>后面。指令必须绑定到一个标签上去，可以绑定到 <strong>&lt;template&gt;</strong> 不可见的元素上去，方便控制。</li>
</ul>
<h3 id="v-show">v-show</h3>
<ul>
<li>我们还可以通过<strong>v-show</strong>来控制元素的显示与隐藏</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
    &lt;div id=&quot;app4&quot;&gt;
        &lt;p v-show=&quot;show&quot;&gt;你现在可以看到我&lt;/p&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
    var app4=new Vue({
        el:&quot;#app3&quot;, //指定绑定的目标dom元素的Id
        //绑定数据源
        data:{
            show:true
        }
    });
&lt;/script&gt;  
</code></pre>
<ul>
<li>我们发现<strong>v-show</strong>指令中，当<strong>data</strong>中<strong>show:false</strong>时候，元素行内样式中是<strong>display：none</strong></li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://s2.ax1x.com/2019/04/26/EmUOv6.png" alt="v-if与v-show" loading="lazy"></figure>
<ul>
<li>
<p>由此可以说<strong>v-if</strong>才是真正的条件渲染，当条件不满足时候，标签并不会去渲染，直至满足条件，而<strong>v-show</strong>是先把元素渲染出来，如果不满足条件，则<strong>dispaly=none</strong></p>
</li>
<li>
<p>一般来说，<strong>v-if</strong> 有更高的切换开销，而 <strong>v-show</strong> 有更高的初始渲染开销。因此，如果需要非常频繁地切换，则使用 <strong>v-show</strong> 较好；如果在运行时条件很少改变，则使用 <strong>v-if</strong> 较好。</p>
</li>
</ul>
<h3 id="v-for">v-for</h3>
<ul>
<li>Vue还提供了列表循环指令</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
    &lt;div id=&quot;app5&quot;&gt;
        &lt;ul v-for=&quot;item in list&quot;&gt;
            &lt;li v-bind:data-id=&quot;item.id&quot;&gt;{{item.name}}&lt;/li&gt;
        &lt;/ul&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
     var app5=new Vue({
        el:&quot;#app5&quot;,
        data:{
            list:[
                {id:1,name:&quot;js&quot;},
                {id:2,name:&quot;jquery&quot;},
                {id:3,name:&quot;vue&quot;},
            ]
        }
    })
&lt;/script&gt;  
</code></pre>
<ul>
<li>我们可以在控制台输入<strong>app5.list.push({id:4,name:&quot;zepto&quot;})<strong>可以看到，列表追加了一行。同时通过</strong>v-bind</strong>给标签加了一个<strong>data-id</strong>属性</li>
</ul>
<h3 id="v-on">v-on</h3>
<ul>
<li>为了让用户和你的应用进行交互，我们可以用 v-on 指令添加一个事件监听器，通过它调用在 Vue 实例中定义的方法：</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
    &lt;div id=&quot;app6&quot;&gt;
      &lt;button v-on:click=&quot;ClickMe&quot;&gt;{{message}}&lt;/button&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
    var app6=new Vue({
        el:&quot;#app6&quot;,
        data:{
            message:&quot;请点击我&quot;
        },
        methods:{
            ClickMe:function(){
                this.message=&quot;狗子，你变了！&quot;;
                console.log(1)
            }
        }
    });
&lt;/script&gt;
</code></pre>
<ul>
<li>在修改标签内容时候，我们并没有像<strong>jQuery</strong>一样去操作DMO,所有的 DOM 操作都由 Vue 来处理。同时v-on也可以进行<strong>简写</strong></li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
    &lt;div id=&quot;app6&quot;&gt;
       &lt;!-- 完整语法 --&gt;
      &lt;button v-on:click=&quot;ClickMe&quot;&gt;{{message}}&lt;/button&gt;
      &lt;!-- 缩写 --&gt;
      &lt;button @click=&quot;ClickMe&quot;&gt;{{message}}&lt;/button&gt;
    &lt;/div&gt;
&lt;/body&gt;
</code></pre>
<h3 id="v-model">v-model</h3>
<ul>
<li>Vue 还提供了 v-model 指令，它能轻松实现表单输入和应用状态之间的双向绑定。</li>
</ul>
<pre><code class="language-Html">&lt;body&gt;
    &lt;div id=&quot;app7&quot;&gt;
        &lt;p&gt;{{message}}&lt;/p&gt;
        &lt;input type=&quot;text&quot; v-model=&quot;message&quot;&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;script src=&quot;https://cdn.jsdelivr.net/npm/vue&quot;&gt;&lt;/script&gt;
&lt;script&gt;
    var app7=new Vue({
       el:&quot;#app7&quot;,
       data:{
           message:&quot;hellow&quot;
       }
   })
&lt;/script&gt;
</code></pre>
<h2 id="组件">组件</h2>
<ul>
<li>组件系统是 Vue 的另一个重要概念，因为它是一种抽象，允许我们使用小型、独立和通常可复用的组件构建大型应用。我们所写的页面都可以分割成一个个组件，比如头部，轮播，尾部等...</li>
</ul>
<h3 id="创建组件">创建组件</h3>
<ul>
<li>在Vue中，创建组件就是预定义了一个Vue的<strong>实例</strong>。</li>
</ul>
<pre><code class="language-Html">    &lt;script&gt;
    //创建一个名字为todo-item的组件
    Vue.component('todo-item',{
        template:&quot;&lt;li&gt;这是一个实例&lt;/li&gt;&quot;
    });
    &lt;/script&gt;
</code></pre>
<ul>
<li>如果这样使用只能渲染相同文本，所以可以通过定义一个<strong>prop</strong>可以接收从父组件传过来的数据</li>
</ul>
<pre><code class="language-Html">    &lt;div id=&quot;app&quot;&gt;
        &lt;ol&gt;
            &lt;todo-item v-for='item in list' v-bind:todo='item' v-bind:key='item.id'&gt;

            &lt;/todo-item&gt;
        &lt;/ol&gt;
    &lt;/div&gt;
    &lt;script&gt;
        //定义组件
        Vue.component('todo-item',{
            props:['todo'],
            template:&quot;&lt;li&gt;{{todo.text}}&lt;/li&gt;&quot;
        });

        var app=new Vue({
            el:&quot;#app&quot;,
            data:{
                list:[
                    {id:1,text:&quot;hellow world&quot;},
                    {id:2,text:&quot;hellow&quot;},
                    {id:3,text:&quot;hi&quot;},
                    {id:4,text:&quot;world&quot;},
                ]
            }
        })
    &lt;/script&gt;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[搭建shadowsocks服务]]></title>
        <id>https://lonjinup.github.io/post/shadowsocks</id>
        <link href="https://lonjinup.github.io/post/shadowsocks">
        </link>
        <updated>2019-08-01T05:24:58.000Z</updated>
        <content type="html"><![CDATA[<h1 id="shadowsocks">shadowsocks</h1>
<pre><code>之前一直是公司后端用公司服务器搭建的，网速慢的要死，准备买个星域的，但是网站老打不开，还是自己搭一个吧，价格也不贵，一个月折合人民币￥25，网速也不慢。
</code></pre>
<h2 id="一-购买服务器">一、购买服务器</h2>
<ul>
<li>
<p>国外的服务器比较多，像我们常见的就有搬瓦工，vultr，linode 亚马逊等，这里我选择<a href="https://app.cloudcone.com">cloudcone</a>。</p>
<h3 id="注册">注册</h3>
</li>
<li>
<p>这里选择<a href="https://app.cloudcone.com">cloudcone</a>进行账号注册。</p>
<h3 id="充值">充值</h3>
</li>
<li>
<p>这里支持支付宝充值，最低充值金额为 <strong>$5</strong>，找到充值入口<img src="https://lonjinup.github.io/post-images/1564638465290.png" alt="" loading="lazy"></p>
</li>
<li>
<p>输入金额支付宝支付<br>
<img src="https://lonjinup.github.io/post-images/1564638814320.png" alt="" loading="lazy"></p>
<h3 id="购买">购买</h3>
</li>
<li>
<p>首页点击新建<br>
<img src="https://lonjinup.github.io/post-images/1564638680586.png" alt="" loading="lazy"></p>
</li>
<li>
<p>按图上配置即可，只需$3.71。<br>
<img src="https://lonjinup.github.io/post-images/1564639093205.png" alt="" loading="lazy"></p>
</li>
</ul>
<blockquote>
<pre><code>这时候购买完毕就可以开始搭建啦，服务器IP地址和密码等信息会下发到邮箱。
</code></pre>
</blockquote>
<h2 id="二-搭建">二、搭建</h2>
<h3 id="准备工具">准备工具</h3>
<h4 id="xshell6"><a href="https://pan.baidu.com/s/1IQcvbkbNWfs44_PDxQhxtQ#list/path=%2F">Xshell6</a>，百度网盘下载。提取码<strong>y4tq</strong></h4>
<h4 id="shadowsocks客户端">shadowsocks客户端</h4>
<p><a href="https://github.com/shadowsocks/shadowsocks-windows">Windows客户端</a><br>
<a href="https://github.com/shadowsocks/ShadowsocksX-NG">Mac客户端</a><br>
<a href="https://github.com/shadowsocks/shadowsocks-android">Android客户端</a><br>
iOS可以用美区ID下载<strong>Potatso Lite</strong>，美国账号可以去万能的tb购买。</p>
<h3 id="服务器">服务器</h3>
<h4 id="连接服务器">连接服务器</h4>
<ul>
<li>安装好<strong>Xshell</strong>，通过ip地址和密码连接服务器。这里就不截图了。</li>
</ul>
<h4 id="安装python-pip">安装python-pip</h4>
<ul>
<li>安装 python-pip<br>
<code>yum -y install python-pip</code></li>
</ul>
<blockquote>
<p>如果提示No package python-pip available.Error: Nothing to do因为没有此rpm包，此包包含在pel源里面,执行如下代码:</p>
</blockquote>
<p><code>rpm -ivh http://dl.fedoraproject.org/pub/epel/6/i386/epel-release-6-8.noarch.rpm</code></p>
<ul>
<li>安装成功后会提示Complete! 之后清除一下缓存<br>
<code>yum clean al</code></li>
</ul>
<h4 id="安装shadowsocks">安装shadowsocks</h4>
<p><code>pip install shadowsocks</code></p>
<h4 id="配置信息">配置信息</h4>
<ul>
<li>新建文件<br>
<code>vim /etc/shadowsocks.json</code></li>
<li>输入配置信息</li>
</ul>
<pre><code>{
  &quot;server&quot;: &quot;服务器IP&quot;,
  &quot;port_password&quot;: {
    &quot;端口&quot;: &quot;密码&quot;
  },
  &quot;local_address&quot;: &quot;127.0.0.1&quot;,
  &quot;local_port&quot;: 1080,
  &quot;timeout&quot;: 300,
  &quot;method&quot;: &quot;aes-256-cfb&quot;
}
</code></pre>
<ul>
<li>编辑好后esc退出输入:wq!保存。</li>
</ul>
<h4 id="运行">运行</h4>
<ul>
<li>要在前台运行:<br>
<code>ssserver -c /etc/shadowsocks.json</code></li>
<li>要在后台运行：<br>
<code>ssserver -c /etc/shadowsocks.json -d start</code><br>
<code>ssserver -c /etc/shadowsocks.json -d stop</code></li>
</ul>
<h2 id="三客户端连接">三.客户端连接</h2>
<ul>
<li>打开客户端，输入ip、端口、选择加盟方式、点击确定即可连接，快试试能不能打开<a href="https://www.google.com/search?ei=24VCXdm0Dtf1-gSdi6iQDg&amp;q=pronhub&amp;oq=pronhub&amp;gs_l=psy-ab.3..35i39.2258667.2261361..2261596...1.0..0.818.2559.2-5j0j1j0j1......0....1..gws-wiz.....10..0i71j0.v5vb1A7VS1c&amp;ved=0ahUKEwiZjvL3hOHjAhXXup4KHZ0FCuIQ4dUDCAo&amp;uact=5">Google</a>吧！</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[一个合格前端需要掌握的知识点]]></title>
        <id>https://lonjinup.github.io/post/yi-ge-he-ge-qian-duan-xu-yao-zhang-wo-de-zhi-shi-dian</id>
        <link href="https://lonjinup.github.io/post/yi-ge-he-ge-qian-duan-xu-yao-zhang-wo-de-zhi-shi-dian">
        </link>
        <updated>2019-07-28T15:04:37.000Z</updated>
        <content type="html"><![CDATA[<h1 id="前言">前言</h1>
<pre><code>	大家肯定有时候和我一样，每次在各大论坛、公众号看到好文章就会点赞搜藏，但其实(大家懂)......
	这次准备把我之前搜藏的一些好文章以及各种资源做个记录，方便每日学习。文章内容准备涉及到HTML、CSS、JavaScript、Vue、浏览器相关 、前端面试常见问题、各种好玩的插件。
	同时自己也立一个flag，今年年底争取把JavaScript以及ES6语法掌握到位，同时再把VUE相关知识学习一下。最近也在巩固(重学)JavaScript，把之前落灰的📕《JavaScript权威指南》拿了出来，争取年底能读完吧，每日进步一点点。
</code></pre>
<h2 id="html-css">HTML  CSS</h2>
<p><a href="http://www.ruanyifeng.com/blog/2015/07/flex-grammar.html">CSS中flex布局</a></p>
<p><a href="https://juejin.im/post/5d3946335188255ac45f7331">grid网格布局</a></p>
<p><a href="https://juejin.im/post/5c939345e51d452c4323a65b">一些有趣的CSS魔法和布局（上）</a></p>
<p><a href="https://juejin.im/post/5c93964e6fb9a070c5511160">一些有趣的CSS魔法和布局（下）</a></p>
<p><a href="https://juejin.im/post/5d39d27cf265da1bc14b6f47">CSS开发必会的16个开发技巧</a></p>
<p><a href="https://juejin.im/post/5d3eca78e51d4561cb5dde12">你未必知道的49个CSS知识点</a></p>
<p><a href="https://juejin.im/post/5cdd178ee51d456e811d279b">2019年了，你还不会CSS动画？</a></p>
<h2 id="javascript">JavaScript</h2>
<p><a href="https://juejin.im/post/5b91d3be5188255c95380b5e">前端跨域相关知识</a></p>
<h2 id="vue">VUE</h2>
<p><a href="https://juejin.im/post/5ad10800f265da23826e681e">如何解释VUE的生命周期才能让面试官满意</a></p>
<p><a href="https://juejin.im/post/5d229bfc5188252d707f3ac6">深入浅出Vue响应式原理（完整版）</a></p>
<p><a href="https://juejin.im/post/5cde0b43f265da03867e78d3">Vue 组件间通信六种方式</a></p>
<h2 id="浏览器">浏览器</h2>
<h2 id="面试相关">面试相关</h2>
<p><a href="https://juejin.im/post/5c6ad9fde51d453c356e37d1">如何横扫 Javascript 面试核心考点(基础版)</a></p>
<h2 id="好玩插件应用">好玩插件|应用</h2>
<p><a href="https://gridea.dev/">Gridea:一个静态博客写作客户端(支持github、coding)</a></p>
<p><a href="https://juejin.im/post/5d21ff70f265da1bcc196468">MAC电脑好用软件</a></p>
]]></content>
    </entry>
</feed>